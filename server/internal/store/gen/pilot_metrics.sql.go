// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.0
// source: pilot_metrics.sql

package gen

import (
	"context"
)

const countActiveFlags = `-- name: CountActiveFlags :one
SELECT count(*) FROM risk_flags WHERE is_active
`

// M — поднятые (активные) сигналы.
func (q *Queries) CountActiveFlags(ctx context.Context) (int64, error) {
	row := q.db.QueryRow(ctx, countActiveFlags)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const countMarkedContracts = `-- name: CountMarkedContracts :one

SELECT count(*) FROM contracts WHERE NOT is_deleted
`

// Метрики этапа 5 для pilot-one-pager (Story 5.6, AC-3). Честные count'ы по проекции; источник данных
// (синтетика/интерим/живой ows) штампуется артефактом, не выдаётся за пилотный результат.
// N — размеченные контракты (импортированы в проекцию, не помечены удалёнными).
func (q *Queries) CountMarkedContracts(ctx context.Context) (int64, error) {
	row := q.db.QueryRow(ctx, countMarkedContracts)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const countRecomputableFlags = `-- name: CountRecomputableFlags :one
SELECT count(*) FROM risk_flags
WHERE is_active AND methodology_version <> '' AND evidence <> '{}'::jsonb
`

// X — активные сигналы, ПЕРЕСЧИТЫВАЕМЫЕ третьим лицом по опубликованной методике: непустые methodology_version
// и evidence (оба NOT NULL по схеме; вырожденные ” / '{}' не пересчитываемы). [Story 5.6 OQ#3 ✅ вариант (а)]
func (q *Queries) CountRecomputableFlags(ctx context.Context) (int64, error) {
	row := q.db.QueryRow(ctx, countRecomputableFlags)
	var count int64
	err := row.Scan(&count)
	return count, err
}
