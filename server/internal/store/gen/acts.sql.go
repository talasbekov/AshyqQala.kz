// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.0
// source: acts.sql

package gen

import (
	"context"
)

const getLatestActByContractID = `-- name: GetLatestActByContractID :one
SELECT id, contract_id, goszakup_act_id, act_date, signer_info, source_url, imported_at, updated_at
FROM acts
WHERE contract_id = $1
ORDER BY act_date DESC NULLS LAST, id DESC
LIMIT 1
`

// Последний акт по контракту (internal contract_id) для карточки (Story 5.1, FR-11). Детерминированный
// порядок: наибольшая act_date (NULL — в конец), затем id. Не найдено → pgx.ErrNoRows (честное «нет акта»,
// карточка показывает блок акта в состоянии no_data, НЕ выдумывает дату/подписанта).
func (q *Queries) GetLatestActByContractID(ctx context.Context, contractID int64) (Act, error) {
	row := q.db.QueryRow(ctx, getLatestActByContractID, contractID)
	var i Act
	err := row.Scan(
		&i.ID,
		&i.ContractID,
		&i.GoszakupActID,
		&i.ActDate,
		&i.SignerInfo,
		&i.SourceUrl,
		&i.ImportedAt,
		&i.UpdatedAt,
	)
	return i, err
}
