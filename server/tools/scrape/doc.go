// Package scrape — ВРЕМЕННЫЙ (трек «Парсер-мост», Story 0.6) парсер ПУБЛИЧНОГО портала
// goszakup.gov.kz как источник ЛОТОВ Астаны без токена. ОДНОРАЗОВО, НЕ ПРОД.
//
// ⚠️ §6.1/§6.4 конкурсного документа: парсинг — осознанное ВРЕМЕННОЕ отклонение от принципа
// «официальный канал, не парсинг». Ратифицирован Sprint Change Proposal 2026-06-20; §6.1/§6.4-override
// согласован владельцем 2026-06-23. Изоляция НЕСУЩАЯ: весь исполняемый код пакета — за build-tag
// `scrape` (вне дефолтного билда), поэтому cmd/api и cmd/importer физически не могут его импортировать
// (CI go-list-страж AR-27, см. internal/arch: TestHotPathDoesNotImportScrape).
//
// ОБРАТИМОСТЬ (AC3): источник структурно удовлетворяет internal/goszakup.Source (тот же Name()+Fetch),
// но НЕ импортирует goszakup — swap scrape→ows = один флаг Source, downstream (decode→lots→geo→map)
// не меняется. КРИТЕРИЙ УДАЛЕНИЯ: получен GOSZAKUP_TOKEN → ows-источник (Story 2.1) заменяет scrape;
// этот пакет и интерим-команда (cmd/interim-import) удаляются. Путь восстановления §6.1/§6.4 — выше.
//
// ПОТОЛОК (эмпирически, Story 0.1): ТОЛЬКО лоты (договоры/участники/РНУ/journal требуют токен);
// выборка keyword-смещена (round-robin дорога/вода) → направления непоказательны, метить «предв.».
package scrape
