# Deferred Work

## Deferred from: code review of 3-5-превью-объекта-и-переход-в-карточку-fr-8 (2026-07-03)

- **Модальный стек «методика + форма ошибки» на карточке контракта против UX-DR34/39 (решение владельца 2026-07-03: отложить — дефолт-выбор при afk, прецедент AI-3)** — `ContractCard.tsx`: onReportError не сбрасывает methTarget → форма поверх методики (2 уровня; epics.md:404,417-419 запрещают стеки глубже одного). Пред-существующее с 5.3/5.4; с `useDialogFocus` работает корректно (Escape — только верхний слой, фокус-цепочка цела — e2e `contract.spec.ts` «стек слоёв» это закрепляет). Унификация с D7-паттерном превью (замена слоя: `setMethTarget(null)` в двух onReportError) — при фронт-полировке; альтернатива — узаконить стек правкой UX-DR34.
- **Почти-совпадающие точки (>ε=0.11 м, но < размера маркера) не группируются в превью-список и недостижимы тапом** (`web/src/features/map/objects.ts` coordKey/`MapView.tsx`) — AC3 3.5 закрывает БИТ-ИДЕНТИЧНЫЕ координаты (`toFixed(6)`-бакет); пары в 1–2 м (разные результаты геокодера по одному дому) кластер не разводит зумом, `allCoincident=false` → обычный зум → наложенные маркеры, нижний недостижим. Уточняет существующий defer 3.4 «spiderfy» (:341): решить ε-окрестностью (не бакетом) ИЛИ spiderfy на живых данных Epic 3 / в 3.8.
- **Веер per-row `useContract`-запросов превью-списка без капа/ленивости** (`web/src/features/map/PreviewSheet.tsx` PreviewListRow) — N совпадающих объектов → N одновременных GET /api/contracts/{id} + простыня «Загрузка…». Пилотный объём (2–6) терпим (прецедент compute-on-read 6.4/3.4); при живых данных с десятками контрактов на адрес — кап/виртуализация/lazy-фетч видимых рядов.
- **Форма «Сообщить об ошибке» незакрываема на всё время зависшего POST** (`web/src/features/share/ReportErrorForm.tsx` guardedClose) — isPending-гард (deferred:63) без таймаута/AbortSignal у `postErrorReport`: повисшая сеть держит юзера в модалке с focus-trap минуты (браузерный дефолт). Активировать бандлом с сетевым харднингом 5.4 (rate-limit/антиспам): таймаут запроса + Abort + разблокировка закрытия по таймауту.
- **ErrorBoundary-fallback методики, смонтированный ПОД открытой формой, становится top стека диалогов** (`web/src/shared/ui/useDialogFocus.ts` + `ContractCard.tsx`) — если MethodologyDialog бросает на ре-рендере, пока форма ошибки открыта поверх, fallback push'ится верхним: Escape закрывает невидимый слой, initial focus крадётся из формы. Двойной-сбой экзотика; при следующем касании хука — push с учётом визуального порядка ИЛИ isTop-гейт initial focus.
- **Refetch карты в 300мс-окне после открытия превью может сбросить previewObjects и снести форму на лету POST** (`web/src/features/map/MapView.tsx` reset-эффект selected) — debounced bbox от последнего moveend приходит после открытия листа/формы; исчезновение объекта из выдачи закрывает всё дерево превью, юзер не видит исход отправки. Узкое окно; бандл с харднингом POST (выше): не сбрасывать при открытом overlay ИЛИ поднять pending выше листа.
- **«Скелетон» превью — текстовая строка «Загрузка…», не скелетон под раскладку** (`PreviewSheet.tsx`) — UX-DR32 требует скелетоны под раскладку; честно, но не скелетон мини-карточки. Фронт-полировка единым проходом (решение владельца 2026-07-01).
- **Скролл страницы под модальными слоями не блокируется** (все диалоги: лист/методика/форма — `aq-sheet-backdrop`/`aq-meth-backdrop`) — пред-существующий паттерн с 1.9/5.3/5.4, 3.5 лишь наследует; body scroll-lock (`overflow:hidden` на время слоя) — фронт-полировка.


## Deferred from: code review of 6-1-фильтрация-контрактов (2026-06-29)

- **Нет индекса под keyset `ORDER BY c.sign_date DESC NULLS LAST, c.goszakup_contract_id` + фасеты** (`server/internal/store/queries/contracts.sql` `ListContracts`; миграции) — проверено: на `contracts` есть только `contracts_supplier_org_idx` (0014); индексов под `sign_date`/`direction`/`amount_tng` НЕТ. `risk_flags_active_idx (contract_id) WHERE is_active` (0007) покрывает `has_active_flag`-EXISTS. На каждую страницу — full-scan + sort. Риск по NFR-1 (поиск <1с p95) при росте; при пилотном объёме Астаны (OQ-1 ещё открыт — щедрый page-size держит типовой запрос в одну страницу) терпимо. Активировать при росте/закрытии OQ-1: отдельная perf-миграция с составным/частичным индексом под сортировку и горячие фасеты.
- **`ResolveSupplierOrgID` не фильтрует `AND NOT is_deleted`** (`server/internal/store/queries/contracts.sql`) — соседний `GetOrganizationByBIN` скрывает удалённые, этот — нет. Влияние НИЗКОЕ: `organizations.bin` = `UNIQUE` (0012), поэтому «произвольная строка при дубле» невозможна; сами контракты независимо режутся `NOT c.is_deleted`. Остаётся лишь несогласованность с конвенцией. Активировать: добавить `AND NOT is_deleted` для единообразия (с регенерацией sqlc) при следующей правке `contracts.sql`.
- **Поведение pgx «nil-slice → SQL NULL» («все направления») покрыто только integration-тестом** (`server/internal/httpapi/contracts_list.go`; `gen/contracts.sql.go` `Directions []string`) — корректность ветки «фасет направления не задан» держится на кодировании `nil []string` как `NULL` (иначе `ANY('{}')` вернёт пусто). Юнит-тесты `go test` это не ловят (за тегом `integration`, нужна БД). Активировать: добавить unit/contract-страж, чтобы смена версии/поведения pgx по nil-slice ловилась в обычном CI.

## Deferred from: code review of 5-7-per-surface-рендер-тест-нейтральности-web-og-cross-surface-snapshot (2026-06-26)

- **Полноценный component-render web (FlagBadge)** (`web/src/features/contract/badgeFramePresence.test.ts`) — рамка-позитив на web проверяется реконструкцией строки + сверкой с golden-литералом (P1), НЕ рендером `FlagBadge.tsx`. Регрессия в самом компоненте (выпавшая рамка в raised-ветке) ловится только косвенно. Полный render-тест требует jsdom/@testing-library (не установлены — повтор defer 5.1/5.3). Активировать при установке jsdom / Playwright-покрытии бейджа.
- **Path-filter CI: glossary-only PR не со-триггерит делегированные стражи** (`.github/workflows/{ci-server,ci-web}.yml`) — `ci-server` триггерится на `server/**`, `ci-web` на `web/**`; PR, меняющий ТОЛЬКО `registry/values/glossary-*.json`, может не запустить og-version-пин и web↔registry parity (делегированные элементы cross-surface карты). ПРЕД-СУЩЕСТВУЮЩИЙ инфра-долг (не введён 5.7). `-count=1` в check-registry (P2) частично закрывает кэш-риск; полное — добавить `registry/**` в path-фильтры ci-server/ci-web ИЛИ отдельный cross-surface CI-job.

## Deferred from: code review of 5-6-перманентная-ссылка-на-дату-экспорт-evidence-и-pilot-one-pager (2026-06-26)

- **Семантика N («размечено»)** (`server/internal/store/queries/pilot_metrics.sql`) — `CountMarkedContracts` = все неудалённые контракты (импортированы в проекцию). Если интент метрики = «прошедшие расчёт флагов», счёт завышается ещё-не-обработанными. Документированная трактовка; уточнить при зрелом compute-пайплайне (Epic 2).
- **M=0/X=0 при N>0 как факт** (`server/cmd/pilot-onepager/report.go`) — `honestCount` применён к OQ1, но не к M/X; «оценено, 0 сигналов» не отличить от «флаги не считались». Нет compute-tracking для различения; внутренний артефакт. Активировать, когда импорт начнёт помечать «расчёт выполнен».
- **`as_of` штампуется в URL, но на чтении не потребляется** (`web/.../permalink.ts`, `ContractRoute.tsx`) — декоративный дисплей-штамп даты; дрейф детектится только по `?mv`. By-design (OQ#1: слайс не обещан замороженным). Активировать при истинной заморозке (snapshot_id).
- **Дрейф детектится только по оси методики; data-drift молчит** (`server/internal/httpapi/contracts.go` `Get`) — при неизменной версии, но повторном импорте (UPSERT last-import-wins) карточка по permalink показывает текущие данные без баннера. Owner-approved OQ#1: детект data-drift без snapshot невозможен по конструкции. Активировать при snapshot-сторе (пилот).
- **`honestCount(0)`: измеренный 0 = «нет данных»** (`server/cmd/pilot-onepager/report.go`) — реальный «0 контрактов в окне» неотличим от «не измеряли». Перестраховка в пользу честности; пересмотреть, если потребуется отличать measured-0.
- **Web drift-баннер без компонентного теста** (`web/src/features/contract/ContractRoute.tsx`) — покрыты `buildPermalink` (unit) + i18n parity/neutrality; JSX-условие баннера без render-теста (jsdom/@testing-library не установлены — повтор defer 5.1/5.3). Активировать при установке jsdom / Playwright-покрытии.
- **`verdict_reason`/`data_source` verbatim без `FindTaboo` на рендере** (`server/cmd/pilot-onepager/report.go`) — нейтральность проверяется тестом на фиксированной выборке, не на рантайм-выводе. Источник — контролируемый stage0-audit (низкий риск); CLI не импортирует registry. Активировать, если one-pager начнёт встраивать неконтролируемый текст.

## Deferred from: dev of 5-6-перманентная-ссылка-на-дату-экспорт-evidence-и-pilot-one-pager (2026-06-26)

- **Истинная заморозка снапшота (`snapshot_id` + append-only дамп evidence/state)** — OQ#1 ✅ решено в пользу
  «штамп+баннер дрейфа» (истории снапшотов в MVP нет; тяжёлый стор отложен архитектурой до пилота, architecture.md:337).
  Permalink честно показывает ТЕКУЩЕЕ состояние + баннер «методика сменилась», но НЕ реконструирует прошлое. Активировать
  при пилоте: колонка `snapshot_id` (НЕ в ключе уникальности) + дешёвый дамп → реальная реконструкция «карточка на дату X».
- **Интеграция на реальном Postgres (Story 5.6)** — НЕ прогнана здесь (БД не поднята): `curl` evidence-эндпоинта
  (`/api/contracts/{id}/flags/{ft}/evidence.{json,txt}`) + прогон `pilot-onepager` против БД (count-запросы N/M/X) + запись
  `docs/ops/pilot-onepager-<date>.md`. Эндпоинты не добавляют новой схемы карточки (переиспускают протестированную проекцию);
  count-запросы тривиальны. Прогнать на CI / при демо-подготовке.
- **Артефакт `pilot-onepager-*.md` НЕ закоммичен** — генерируется при демо против реальной/интерим-БД (синтетические числа
  не выдаём за пилотный результат). Render golden-тестирован; `readVerdict` тестирован против закоммиченного вердикта.
- **`Accept-Language` для локали экспорта** — реализован `?lang=ru|kk` + дефолт KK; `Accept-Language` как доп. фолбэк — задел.
- **`.txt`-экспорт вне OpenAPI** — намеренно (text/plain, вне JSON-арбитра; прецедент 5.5 OG-HTML). `evidence.json` — в арбитре.
- **`/metrics` гейтинг (повтор defer 1.1)** — one-pager сделан артефакт-генератором (НЕ публичный эндпоинт), чтобы НЕ
  светить метрики наружу до гейтинга `/metrics` (Caddy allow внутренних IP / admin-listener).

## Deferred from: code review of 5-5-og-шаринг-карточки-серверный-рендер (2026-06-26)

- **Канонический `/contracts/{id}` не отдаёт OG-теги краулерам** — уже отслежено выше (dev-defer 5.5, «Bot-detection OQ#2»); шаринговый OG-URL = `/og/contracts/{id}`. Влияет на реальную шарабельность (FR-27/SM-5), если пользователь шлёт URL из адресной строки SPA.
- **На картинке/в теге печатается ГЛОБАЛЬНАЯ `methodology_version`, не per-flag из evidence** (`server/internal/og/image.go:103-106`, `meta.go:233`) — `h.Version = params.MethodologyVersion` (каноническая); каждый raised-флаг несёт свою `MethodologyVersion` в evidence (`contract_flags.go`). Если флаг посчитан под более старой методикой — картинка покажет текущую глобальную (перекрёстный инвариант версии). AC-2 требует лишь присутствия версии → не нарушение. Активировать вместе с per-flag прозой/числами (**Epic 4**).
- **Амбер-пилюля может наехать на линейку подвала при 3-строчном заголовке** (`server/internal/og/image.go:122,137`) — `pillTop = amountY + 36`, при 3 строках `amountY=418` → низ пилюли ≈ линейка подвала (`ogH-110=520`). `fitLine` решает только горизонталь; вертикального арбитража нет. Косметика; подтвердить визуально связку «3-строчный заголовок + raised-пилюля» (dev визуально проверял raised/noflags, но не обязательно этот combo). Активировать: вертикальный fit/клэмп раскладки.
- **Подвал картинки: короткий URL (слева) и ссылки методика/ошибка (справа) не арбитрируются по ширине** (`server/internal/og/image.go:138-141`) — длинный `goszakup_contract_id` → «ashyqqala.kz/contracts/<длинный-id>» пересекается с правым блоком ссылок (нет `fitLine`/усечения на подвальном URL). Косметика. Активировать: усечение/перенос подвального URL.
- **На растре показан только ПЕРВЫЙ raised-флаг, в HTML — все** (`server/internal/og/image.go:124-129` `break` vs `meta.go:96-104` `Join`) — при нескольких активных сигналах пересланный растр недо-раскрывает их количество. Не фактологическая ошибка (короткий URL ведёт к полной карточке), per-surface расхождение объёма. Активировать: на картинке показывать счётчик «+N сигналов» или несколько пилюль.
- **Одиночное слово шире `maxW` не переносится/не хард-брейкается → выезд за край картинки** (`server/internal/og/image.go` `wrapText`) — слово, не попавшее на последнюю строку, рисуется целиком за `ogW-ogPad`. Редко (54pt, >~30 символов без пробела); частично снимается фиксом whitespace-split (NBSP-склейка перестанет давать «гигантское слово»). Активировать: хард-брейк сверхдлинного токена по глифам.
- **`image.png` кэшируется (`Cache-Control: public, max-age=3600`) с чтением cookie `aq-lang` без `Vary: Cookie`** (`server/internal/og/image.go:88`) — прямой запрос `image.png` без `?lang`, но с cookie, при кэше по URL мог бы отдать картинку не на том языке. Краулеры cookie не шлют, канонический `og:image` всегда несёт `?lang` → риск низкий. Активировать: `Vary: Cookie` ИЛИ игнорировать cookie на image-эндпоинте (только query).

## Deferred from: dev of 5-5-og-шаринг-карточки-серверный-рендер (2026-06-26)

- **✅ T5 (PNG OG-картинка) — СДЕЛАНА** (одобрено владельцем 2026-06-26): `og.MetaHandler.Image` + `image.go` на `golang.org/x/image/font/opentype` + встроенный DejaVu Sans (`//go:embed`, `fonts/LICENSE.md`). Глиф-страж (казахские буквы + ₸) + визуальная проверка (kk/ru). QR не делал — достаточно «видимого короткого URL» (AR-14); QR — опц. задел.
- **Per-surface нейтральность ТЕКСТА на картинке** — картинка рисует ТОЛЬКО строки из `render`/glossary (`FlagLine`/`Text`/`subjectText`), которые уже покрыты taboo-стражами (render-нейтральность + `meta_test` per-surface). Отдельный «taboo по растру» не нужен (источник один). Если в картинку добавят прозу мимо render — добавить прогон `FindTaboo` по её строкам.
- **Cross-surface golden — Telegram-плечо** — web↔OG golden сделан (`render/flagline_golden_test.go` + web parity). Тройное web↔OG↔Telegram замкнётся на 7.1 (бота/поверхности Telegram ещё нет). Закрывает часть defer 5-1 (строка ниже): «вторая поверхность OG» теперь есть.
- **Bot-detection на каноническом `/contracts/{id}` (OQ#2)** — сейчас шаринговый OG-URL = `/og/contracts/{id}` (Caddy `handle /og/*`→api). Чтобы и канонический URL карточки отдавал OG-теги краулерам соцсетей — решить позже: детекция по user-agent ИЛИ всегда-SSR-shell. Влияет на UX кнопки «поделиться».
- **`Accept-Language` для OG-локали** — реализованы query `?lang=` + cookie `aq-lang` + дефолт KK; `Accept-Language` как доп. фолбэк — задел (для шаринга достаточно явного выбора).
- **OpenAPI для OG-эндпоинта** — намеренно НЕ добавлен: OG отдаёт `text/html`, вне JSON-wire-арбитра (`openapi.yaml` правит только JSON-контракт; `schema.gen.ts`/`gen/*` не трогались). Если позже понадобится формальная фиксация HTML-путей — отдельное решение.
- **axe-a11y / нагрузочный перф (повтор)** — `@axe-core/*` не установлены (одобрение владельца); перф OG <1c p95 (NFR-1) — на нагрузочной истории. OG-страница минимальна (meta+ссылки), но мера не снята.

## Deferred from: code review of 5-4-канал-сообщить-об-ошибке (2026-06-26)

- **✅ РЕШЕНО Story 3.5 (2026-07-03):** `useDialogFocus` даёт focus-trap + возврат фокуса на триггер; `guardedClose`/disabled блокируют закрытие при `isPending` (таймаут/Abort POST — отдельный defer 3.5). ~~**Возврат фокуса на триггер + закрытие формы во время сабмита** (`web/src/features/share/ReportErrorForm.tsx`) — при закрытии (Escape/кнопка/бэкдроп) фокус не возвращается на открывшую дверь; закрытие на лету POST'а оставляет запрос «в полёте» (запись на сервере есть, юзер без подтверждения, риск повторной отправки). Привязано к полному focus-trap (Story 3.5). Активировать вместе с 3.5: сохранять триггер, возвращать фокус; блокировать/предупреждать закрытие при `isPending`.~~
- **rate-limiter без эвикции ключей (см. также dev-запись ниже)** — после XFF-фикса (правый хоп) усиление подменой устранено; рост по реальным IP остаётся. LRU/периодическая чистка ИЛИ внешний rate-limit (Caddy `trusted_proxies`) — при росте трафика.

## Deferred from: dev of 5-4-канал-сообщить-об-ошибке (2026-06-26)

- **Изоляция интеграционных тестов risk_flags-семейства (общая БД, глобальные счётчики)** (`server/internal/store/projection/{price_per_km,risk_flags,monopoly,rnu}_integration_test.go`) — при полном прогоне `go test -tags=integration ./internal/store/...` против ОДНОЙ общей БД `TestPricePerKMFlag_RaiseAutoClear` и `TestSingleParticipantFlag_RaiseAutoClearIdempotent` падают («active = 2, ожид. 1»): тесты считают ГЛОБАЛЬНОЕ число активных флагов и не изолируют строки друг друга. В ИЗОЛЯЦИИ оба зелёные. ПРЕД-СУЩЕСТВУЮЩЕЕ (не вызвано 5.4: миграция `error_reports` аддитивна, risk_flags не трогался; CI, вероятно, изолирует БД per-package/контейнер). Активировать: TRUNCATE risk_flags в setup каждого теста ИЛИ считать активные флаги ТОЛЬКО по subject_id теста, ИЛИ per-test schema/контейнер.
- **`error_reports` → SM-C2 + `geo_objects.wrong_reported`** — обращение `kind=geo_wrong_point` фиксируется в `error_reports` (5.4), но простановка `wrong_reported` на `geo_objects` и инкремент счётчика SM-C2 на `/metrics` — **Epic 3** (геокуратор `geo_objects` ещё не построен; карта = `interim_geo_lots`). [Source: architecture.md:893-894]
- **Гранты БД для `error_reports` (Directus-очередь)** — таблица создана (миграция 0011); compose-гранты «API только INSERT, Directus полный, importer без доступа» прописать вместе с разводкой грантов проекции/курации (как для прочих кураторских таблиц). [Source: architecture.md:310,416]
- **Telegram deep-link отправитель** — 5.4 строит ЦЕЛЬ deep-link открытия формы по объекту; пуш, который шлёт ссылку — **Epic 7** (бота нет).
- **Антиспам — усиление при росте трафика** — сейчас honeypot + лимит тела + in-memory per-IP rate-limit (карта IP растёт без чистки). При росте: периодическая чистка/LRU лимитера ИЛИ внешний rate-limit (Caddy), captcha/Turnstile при волне спама.

## Deferred from: code review of 5-3-экран-методики-флага (2026-06-26)

- **Go-хендлер `/api/methodology` — defensive-валидация порогов + проверка `writeJSON`-ошибки** (`server/internal/httpapi/methodology.go:34-46`) — хендлер публикует пороги из `methodology_params` без проверки `factor/months/min_sample > 0`; при битом конфиге (factor=0) формула вырождается в «median × 0» (всегда истина). `writeJSON` не проверяет ошибку encode. Сейчас безопасно: params грузятся из иммутабельного валидированного реестра (`methodology_params.v1.yaml`, формат версии/значения проверены при загрузке) → не реальный сценарий данных. Активировать при наполнении DB-реестра / живом пересчёте (Epic 2): sanity-валидация порогов на отдаче + проверка `writeJSON`.
- **✅ РЕШЕНО Story 3.5 (2026-07-03):** новый `MethodologyFallback` (features/flag) на `useDialogFocus` — Escape/бэкдроп/кнопка + возврат фокуса; подключён в ContractCard и PreviewSheet. ~~**ErrorBoundary fallback без Escape/возврата фокуса** (`web/src/features/contract/ContractCard.tsx`) — fallback диалога методики (показывается при render-throw) не обрабатывает Escape и не ставит/возвращает фокус, в отличие от самого `MethodologyDialog`. AT-пользователь на пути восстановления после ошибки теряет фокус. Привязано к полному focus-trap, отложенному в **Story 3.5** (a11y canvas-карты/диалогов) — закрыть вместе.~~
- **`insufficient_monopoly` — мёртвая i18n-строка** (повтор defer 2026-06-25) — рендера нет: монополия недостижима с карточки контракта (`contract_flags.go` `contractFlagTypes`=[single_participant, price_per_km]; монополия/РНУ — субъект-подрядчик, AC5). Forward-compat для карточки подрядчика (**Story 5-2**): подключится при ветке monopoly в `MethodologyDialog`.

## Deferred from: code review of story-5.3 (2026-06-25)

- **monopoly/rnu-методика недостижима с карточки контракта** — AC-2 «разделены min_sample/min_group_contracts» полностью реализуется только с карточкой подрядчика (5-2): `min_group_contracts` уже в `/api/methodology` + i18n (`insufficient_monopoly`), но surface монополии/РНУ — contractor (5-2, заблокирована). На карточке контракта реализован `min_sample` (price_per_km). Активировать с 5-2: ветка monopoly/rnu в MethodologyDialog + рендер `insufficient_monopoly`.
- **`not_published` на экране методики показал бы «недостаточно выборки» (mislabel)** — сейчас НЕДОСТИЖИМ: `resolveContractFlags` не эмитит `not_published` (нет строки→insufficient_data, активная→raised, снятая→not_raised). Если read-side когда-нибудь начнёт отдавать `not_published` (kill-switch на чтении) — добавить отдельную ветку «оценка не публиковалась» (не «недостаточно выборки»).
- **Логика ветвления `MethodologyDialog` не покрыта vitest** — только адаптер (`apiFlag.test.ts`) + 2 e2e-пути. Нужен jsdom/@testing-library/react (не установлены, defer ещё с 5-1; новая зависимость → одобрение владельца). Извлечь ветвление (порог-по-flag_id, рендер воркшита, скрытие при незагруженных params) в чистые функции и покрыть vitest при установке jsdom.
- **`insufficient_monopoly` — мёртвая i18n-строка** (`chrome.methodology`, ru+kk) — парность тестируется, но рендера нет (монополия недостижима). Forward-compat для 5-2; безвредно. Подключится при ветке monopoly в диалоге.

## Deferred from: code review of story-5.1 (2026-06-25)

- **Нет route-level error-boundary вокруг карточки** (`web/src/features/contract/ContractRoute.tsx`) — рендер-throw из `formatDate`/`formatMoney` (напр. дата, прошедшая regex `^\d{4}-\d{2}-\d{2}`, но невалидная: `new Date("2026-13-45")` → Invalid → throw) уронил бы весь маршрут (белый экран) вместо честного «нет данных». Достижимость ≈0 при текущей архитектуре: бэкенд через `fromDate`/`fromTimestamptz` всегда форматирует валидную дату из pgtype; throw-by-design с 1.5. Решение владельца 2026-06-25: defer — рекомендованный app-wide хардненинг. Активировать: добавить ErrorBoundary на уровне маршрутов (честный fallback) при появлении не-Postgres источника данных или живого импорта (Epic 2), где входные даты не гарантированы.
- **«Район»/«Подрядчик» не кликабельные ссылки (AC-1/T6)** — цель ссылки не существует: страница района — Epic 6, карточка подрядчика — Story 5.2; `customer`/`supplier` сейчас честный `no_data`. Живая ссылка вела бы в 404 (нечестно) → plain-text честнее. Активировать: при 5.2 (Подрядчик→карточка) и Epic 6 (Район→страница района) обернуть значение в ссылку, когда таргет существует.
- **`insufficient_data` выводится из отсутствия строки, а не из `comparable_n < min_sample`; `not_published` недостижим на чтении** — read-side `resolveContractFlags` различает только raised/not_raised/insufficient (нет строки). Корень — стор схлопывает «оценено-но-мало-выборки» в «нет строки» (Epic 4). Read-side даёт честный fallback (insufficient, никогда «чисто»). Активировать: compute должен персистить insufficient-evidence (строка с состоянием + sample/min) — follow-up Epic 4; тогда read-side свяжет insufficient с порогом и сможет отдавать not_published на чтении.
- **AC-7 (perf <1c p95) / AC-8 (cross-surface golden) / axe-a11y** — заявлены, не реализованы (честно раскрыто в T8/T11). Нет нагрузочного теста, нет поверхностей OG/Telegram для golden, нет `@axe-core/playwright`/`vitest-axe` (новая зависимость). Активировать на нагрузочной истории / 5.5 / при установке axe.
- **`deviation_factor ≠ 1.5` / округление порога / дефолты `MethodologyDialog`** — `thresholdPerKm` точен только для факторов ≤3 знаков; `MethodologyDialog` дефолтит `factor ?? '1.5'`, `sampleSize ?? 0`, `minSample ?? 5`. При фиксированном 1.5 (текущая методика) всё точно. Активировать на 5.3 (экран методики): убрать дефолты/честно скрывать недостающее, проверять кратность фактора.
- **`writeJSON` глотает ошибку encode + WriteHeader(200) до encode** (`server/internal/httpapi/contracts.go`) — pre-existing (с 1.3). Битый/непарсируемый evidence (`json.RawMessage` re-валидируется при Marshal) дал бы пустое тело со статусом 200. Сейчас недостижимо: `risk_flags.evidence` JSONB NOT NULL пишется валидным `json.Marshal(struct)`. Активировать при живом источнике/курации: `json.Valid(evidence)` перед passthrough или маршалить структуру, а не сырые байты; писать заголовок ПОСЛЕ успешного encode (буфер).
- **`GetLatestActByContractID` — выбор «последнего» акта** (`server/internal/store/queries/acts.sql`) — `ORDER BY act_date DESC NULLS LAST, id DESC`: NULL-датированный акт никогда не выбирается при наличии датированного; tie-break по id (импорт), не по семантике; re-seed после смены internal id контракта не репойнтит `contract_id` акта (UPSERT по `goszakup_act_id` не трогает `contract_id`). Низкое влияние (1 акт на контракт на синтетике). Активировать при живом `/v2/acts` (несколько актов/контракт): уникальность `(contract_id, …)`, recency по `imported_at`, репойнт `contract_id` в UPSERT.
- **`flag_type` вне `contractFlagTypes` молча отбрасывается; i18n raw-key leak для неизвестного flag_id/state** (`contract_flags.go`, `ContractCard.tsx`) — будущий 3-й контракт-флаг исчезнет с карточки без следа; неизвестный `flag_id` отрендерит сырой ключ `flag.<id>.name`. Активировать при добавлении контракт-флага: расширить `contractFlagTypes` + `defaultValue`/диагностика в `t()`.

## Deferred from: implementation of 5-1 (полная карточка контракта) (2026-06-25)

- **✅ РЕШЕНО этой историей:** «store схлопывает честные состояния → реконструировать на чтении (Epic 5)» (заявлено в 4.2/4.5/4.6-ревью) — реализовано `resolveContractFlags` (`server/internal/httpapi/contract_flags.go`): нет строки→`insufficient_data`, снятая→`not_raised`, активная→`raised`. Negative-control тест + e2e + real-DB подтвердили.
- **cross-surface golden snapshot нейтральности (web/OG/Telegram)** — отложен: поверхностей OG (`internal/og`, Story 5.5) и Telegram (Epic 7) ещё НЕТ → сравнивать смысловое ядро не с чем. Per-surface нейтральность web сделана (`web/.../contractCardStrings.test.ts` + backend doc-нейтральность). Активировать при первой второй поверхности (5.5/7.1): golden-snapshot равенства {frame, число, flag_id, methodology_version, locale} между web и OG/Telegram.
- **axe-a11y smoke не прогнан** — `@axe-core/playwright`/`vitest-axe` НЕ установлены (новая зависимость → нужно одобрение владельца). Семантика карточки 5-1 проверена вручную (`article`/`dl`/headings, `lang`-атрибуты, focus-ring токены, ≥44px, статус не только цветом — глиф «!»/«—»). Активировать при установке axe-зависимостей: автоматический a11y-smoke карточки в обеих темах/локалях (NFR-7).
- **`MethodologyDialog` внутренние дефолты + error-boundary** — `factor ?? '1.5'`, `sampleSize ?? 0`, `minSample ?? 5` остаются (на raised-флаге evidence полон → фабрикация не воспроизводится; адаптер `apiFlag.ts` ставит только присутствующие поля). Полный харднинг + error-boundary — на поверхности методики (Story 5.3): дефолты убрать или честно скрывать недостающее, обернуть диалог error-boundary.
- **Заказчик/Подрядчик/Район — живые ссылки** — сейчас `customer`/`supplier` честный `no_data` (нет `organizations`/`supplier_org_id`); «Район» = `kato_code` (страница района — Epic 6, живой ссылки пока нет). Token-готово: поля наполнятся при `organizations` (Epic 2) и станут кликабельными при карточке подрядчика (5.2)/странице района (Epic 6) без изменения wire/карточки.
- **Реальные `/v2/acts` + поля акта (VERIFY -probe)** — акт сейчас на синтетическом seed (`fixtures/seed/acts.sql`). При токене: декод `/v2/acts` (имена полей `act_date`/`signer_info`/`source_url` — VERIFY через `-probe`), импорт в `acts`, тот же блок карточки наполнится реальными данными.

## Deferred from: code review of story-4.6 (2026-06-25)

- **Консолидированный property-тест «insufficient ⟺ comparable_n < min_sample» отсутствует, хотя заявлен** (`server/internal/render/render_property_test.go`) — Completion Notes (стр.179) выдают `TestRenderFlagState_StatesDistinct` (только distinctness честных состояний) за закрытие AC2(б); единого property-теста биконституэнты «insufficient ⟺ недостаточная выборка/нет данных» нет. Инвариант substantively покрыт per-flag тестами 4.2–4.5. Активировать при желании дословно закрыть AC2(б): один property-тест по всем flag_type, что insufficient ⟺ `comparable_n < min_sample`.
- **`allFlagIDs` захардкожен** (`server/internal/render/render_property_test.go:12`) — flag-измерение матрицы нейтральности декоративно (Render игнорирует flagID; локали/состояния выводятся из `registry.AllLocales()/AllFlagStates()`, а флаги — ручной список). 5-й флаг не попал бы в матрицу автоматически. При появлении канонического реестра flag_type в `registry` — выводить список оттуда.
- **`subject_id`-маппинг между export и flag_disputes нигде не сверяется** (`server/internal/export/evidence.go:13-19`, `server/internal/store/projection/flag_disputes.go`) — `contract→contract_id` / `contractor→organization_id` → единый `subject_id` заполняет вызывающий; в коде нет сверки. Вызывающий = admin/Epic 5; добавить контракт/сверку при разводке UI диспутов и экспорта (Epic 5).
- **Нет defensive `json.Valid(f.Evidence)` в `export.Export`** (`server/internal/export/evidence.go:33-49`) — битый evidence ушёл бы в JSON/печатный текст как есть. Evidence пишется проекцией как валидный JSON → сейчас безопасно. Добавить проверку при живом источнике/курации (Epic 2).
- **`counterValue` молча возвращает 0 при ошибке `Counter.Write`** (`server/internal/metrics/metrics.go:69-75`) — маскирует сбой снапшота (дельта в тестах измеримости станет бессмысленной). Простой `Counter.Write` на практике не ошибается. Залогировать/вернуть ошибку при усложнении метрик.
- **`resolved_at` перештампуется при `confirmed→withdrawn`** (`server/internal/store/queries/flag_disputes.sql:572-577`) — `ON CONFLICT DO UPDATE` ставит новый `now()`, теряя время первичного резолва. Минорно; пересмотреть вместе с решением по семантике переходов SM-C1 (см. decision-needed) при разводке живой операторской верификации (Epic 2).

## Deferred from: code review of 4-2-флаг-единственный-участник-fr-19 (2026-06-24)

- **Авто-снятие срабатывает только для контрактов во входном наборе пересчёта** (`server/internal/store/projection/risk_flags.go` `RecomputeSingleParticipant`) — если контракт исчезает из набора входов, его активный флаг НЕ снимается (snятие только для явно присутствующих). Требование «Provider возвращает ПОЛНЫЙ набор» нигде не закреплено → дырявое авто-снятие при частичном наборе. На 4.2 (синтетика, контролируемые входы) безопасно. Активировать при живом источнике (Epic 2, `/v2/trd-buy` + проекции announcements/participants): provider обязан возвращать полный набор контрактов ЛИБО реализовать clear-by-absence (снятие флагов, отсутствующих во входах), напр. через журнал изменений (`/v2/journal`, Story 2.2).
- **Нормализация способа закупки перед `methodExcluded`** (`server/internal/flags/single_participant.go`) — сравнение `EqualFold`+`TrimSpace` устойчиво к регистру (вкл. кириллицу) и крайним пробелам, но НЕ к: неразрывному пробелу ` `, внутренним повторным пробелам, гомоглифам латиница↔кириллица, NFC/NFD-формам Unicode. Живой `/v2/trd-buy` может отдать способ в любой из этих форм → ложный `raised` для законной закупки из одного источника. На 4.2 (синтетика, контролируемые строки) не проявляется. Активировать в Epic 2: нормализация способа закупки в decode-слое (как нормализация наименований к канону, Story 2.3) перед подачей в флаг.
- **Слой хранения risk_flags не различает insufficient_data / not_raised / not_published** (`server/internal/store/projection/risk_flags.go`, `risk_flags.sql`) — чистая `flags.SingleParticipant` различает 4 состояния, но store персистит только `raised` (+снятые); остальные три = «нет строки». Читатель (`Get`→false) не отличит «недостаточно данных» (честное no-data) от «проверено, сигнала нет». Для backend-only 4.2 приемлемо (таблица хранит активные флаги). Активировать в Epic 5 (presentation): на показе различать insufficient_data (нет данных об участниках) от not_raised (есть данные, сигнала нет) — вычислять состояние на чтении из доступности evidence/входов, не схлопывать в «нет флага».

## Deferred from: code review of 4-1-methodology_params-иммутабельный-median-benchmark-движок (2026-06-24)

- **Нет инварианта «methodology_version в YAML == версия в строках methodology_params»; `InsertMethodologyParam` не валидирует формат версии** (`server/internal/store/queries/methodology_params.sql`, `server/internal/store/projection/price_benchmarks.go`) — `methodology.validate` проверяет `^v\d+\.\d+$` только для версии из YAML; `InsertMethodologyParam` принимает любую строку версии. В 4.1 DB-таблица `methodology_params` НЕ наполняется (источник истины — YAML; реестр версий в БД для evidence — дескоуп). Перекрёстный инвариант AC5б («версия в данных == каноническая») заявлен в комментарии `BenchmarkStore`, но в коде не проверяется. Активировать при наполнении DB-реестра (sync YAML→DB для evidence/disputes — Story 4.6) либо при подключении живого пересчёта (Story 2.4): валидировать формат версии на вставке + сверять, что записываемая `methodology_version` существует строкой в `methodology_params`.

## Deferred from: code review of 0-7-гео-привязка-scraped-лотов-batch-nominatim (2026-06-23)

- **Лоссовый адрес геокодера (`title_ru + ", Астана"`)** (`server/tools/scrape/cmd/interim-geocode/main.go`) — `addr()` приклеивает город к НАЗВАНИЮ лота (procurement-тайтл, не адрес: «Реконструкция автодороги по ул. Абая»). Геокодинг такого по сути лоссов; дубль города при уже-присутствующем городе может ухудшить матч. Для интерима покрыто «предв./keyword-bias» + автопокрытие-как-факт (не гейт). Качество адресной строки решает живой `ows`-импорт (адресные поля) / Epic 3 (нормализация адреса перед геокодингом).
- **Нет backoff на HTTP 429/Retry-After** (`server/internal/geo/nominatim.go`) — публичный Nominatim при 429 не уважается (нет паузы по Retry-After); при долгом batch может забанить UA/IP. Для интерима частично покрыто вежливым delay (1100мс) + outage-exit (все-ошибки → exit 1). Полноценный backoff/Retry-After — Epic 3 (Story 3.1) либо self-host Nominatim (AR-22, без публичной политики).
- **Неатомарный partial-write batch** (`server/tools/scrape/cmd/interim-geocode/main.go`) — `UpsertGeoLot` в цикле без транзакции: падение БД на N-м лоте оставляет 0..N-1 записанными. Повтор идемпотентен по `goszakup_lot_id` (самоисцеление). Приемлемо для интерим-тула (как и `interim-import` 0.6); атомарность — при необходимости, не сейчас.

## Deferred from: code review of 0-6-временный-scrape-источник-лотов-за-флагом (2026-06-23)

- **Слабый synthetic natural-key при пустом `anno`** (`server/tools/scrape/scrape.go`) — `goszakup_lot_id` синтезируется из `(anno|name)`; если в первой колонке нет `\d{6,}-\d+` (`anno=""`), две РАЗНЫЕ строки с одинаковым `name_ru` дают один ключ → одна молча выпадает (dedup + UPSERT-overwrite). Дедуп и id имеют одну слабость (нет второй линии защиты). Присущая ограниченность tokenless-скрейпинга (портал реального `lot_id` не отдаёт). РЕШАЕТ критерий удаления: swap `scrape→ows` (Story 2.1) даёт настоящий `lot_id`. До swap — документировано как «предв.».
- **Regex-HTML хрупкость интерим-парсера** (`server/tools/scrape/scrape.go`) — (а) `(?s)…(.*?)</table>` усекает захват на ПЕРВОМ вложенном `</table>` (если строка результата содержит вложенную таблицу — молча меньше лотов); (б) термин помечается `exhausted`, если страница вся из дублей (`len(rows)==0` после `seen`), хотя следующая страница могла нести новые лоты → преждевременная остановка. Присуще regex-парсингу публичной разметки. РЕШАЕТ `ows` API (структурированный JSON) при swap. До swap — HTML-фикстура ловит дрейф известной формы.
- **Точность `amount` scrape→lots** (`server/tools/scrape/scrape.go` + `internal/ingest/decode`) — `parseMoney` отдаёт `float64`, `decode.lotInt` усекает `int64(float64)` (копейки теряются; `>2^53` — потеря точности). Сворачивается в УЖЕ существующий defer «decode value-robustness lots → Story 2.1» (см. ниже, ревью 2-0/1-10): живой декод обязан принимать точные суммы. Для интерим-сумм (целые ₸ < 2^53) на практике безопасно.
- **Неатомарный импорт в БД** (`server/tools/scrape/cmd/interim-import/main.go`) — `UpsertLot` в цикле без транзакции: фейл на N-м лоте оставляет 0..N-1 записанными. Повтор идемпотентен по `goszakup_lot_id` (самоисцеление), exit-код фиксирует фейл. Приемлемо для интерим-тула; при необходимости атомарности — обернуть в `pgx.Tx` (нужды нет, пока источник временный).

## Deferred from: code review of 0-2-машиночитаемый-go-no-go-вердикт-stage0-audit (2026-06-23)

- **-allow-fallback: артефакт go_with_fallback без провенанса владельца/sign-off** (`stage0-audit/verdict.go:175-180`) — флаг переключает реальный No-Go (`coverage < gate`) в `go_with_fallback` + exit 0, но JSON-артефакт не содержит поля «кто/когда подписал». По спеке (Q1) сам флаг = осознанный override, а фиксация владельца+даты делегирована Story 0.4. В Story 0.4 добавить в артефакт поле провенанса (owner + date + reason), чтобы baseline `go_with_fallback` был машинно-доказуем, а не основан только на факте передачи флага.

## Deferred from: code review of 2-0-фундамент-source-file-источник-и-проекция-lots (2026-06-23)

- **decode value-robustness lots (живой ows_v2 → Story 2.1)** (`server/internal/ingest/decode/lots.go:298`) — то же, что для `Contract` (см. ниже, 1-10): `lotInt` берёт только `float64`/`int64`/`int`; живой ows часто отдаёт суммы строками (`"240000000"`) или под `UseNumber()` (`json.Number`) → `Amount` молча станет `nil` (тихая потеря, обходит «не тихий 0» на уровне значения); `float64` теряет точность >2^53. Кандидаты `summ`/`sum`/`total_sum` часто строковые в ows. Story 2.1 (живой декод) обязана принимать строковые/`json.Number` суммы и защищать точность.
- **Дублирование pick-хелперов в decode** (`server/internal/ingest/decode/lots.go`) — `lotPick`/`lotString`/`lotInt` дословно повторяют `pick`/`pickString`/`pickInt` (различие — карта кандидатов). `SchemaHash`/`keysOf` переиспользованы, а эти — скопированы. Обобщить (предикат + карта параметром) при добавлении следующих доменных типов Epic 2, чтобы не плодить копии на каждый ресурс.
- **UPSERT затирает quantity/unit/announcement_id в NULL при смешении источников** (`server/internal/store/queries/lots.sql`) — `ON CONFLICT DO UPDATE SET quantity = EXCLUDED.quantity …` безусловно ставит NULL, т.к. декод эти поля не заполняет. Для S-0 (единый импортёр) безопасно. При смешении источников/кураторских правок → потеря данных; учесть в Story 2.6 (идемпотентность + выживание курации).

## Deferred from: code review of 1-10-общие-контракты-ядра-... (2026-06-23)

- **decode value-robustness (живой ows_v2 → Story 2.1)** (`server/internal/ingest/decode/decode.go`) — `schema_hash` ловит дрейф НАБОРА полей (AC2 ✓), но НЕ дрейф на уровне значения: поле present-но-`null` (`pick` пропускает nil) / число пришло строкой (`pickInt` падает в nil) / `float64→int64` усечение дроби и потеря точности >2^53 → сумма молча теряется (обходит «не тихий 0» на уровне значения). Синтетический golden чист, поэтому не проявляется. Story 2.1 (живой декод реального ows_v2) обязана: различать «поле отсутствует» vs «present-но-неинтерпретируемо» (→ ошибка/honest-state, не nil), принимать строковые числа, защищать точность сумм.
- **Курация: конфликт overrides + валидация Field (Epic 2)** (`server/internal/store/curation/curation.go`) — два override на одно поле разрешаются last-wins молча (в registry есть `StateSourceConflict` ровно для этого); пустой `Field` создаёт мусорный ключ `""` без валидации. Stub минимален; реальная курация (Directus + БД + грантовая граница) в Epic 2 должна детектировать конфликт и валидировать вход.
- **clock zero-value / typed-nil (Epic 4)** (`server/internal/flags/flags.go`) — `Flag` отличает только `Now == nil`; `clock.Fixed{}` (zero-value, «сейчас» = 0001-01-01) и typed-nil `Clock` проходят как валидные. Станет важно, когда дата-зависимые флаги (РНУ авто-снятие) реально используют clock — Epic 4: валидировать ненулевые часы.
- **go-list-страж: расширить покрытие (Epic 4)** (`server/internal/arch/boundaries_test.go`) — `benchmark` сейчас в списке pure-core вакуумно (пустой пакет, нечего проверять); при наполнении benchmark в Epic 4 убедиться, что страж реально валидирует его импорты. `decode`/`clock` намеренно вне pure-core (decode — граница ows→домен, может импортировать goszakup в 2.1; clock легально держит `time`).

## Deferred from: code review of 1-9-вертикальный-срез-3-читаемых-контракта-истории-демо-фундамент (2026-06-23)

- **✅ РЕШЕНО Story 3.5 (2026-07-03, частично):** `useDialogFocus` — trap Tab/Shift+Tab + document-level Escape + возврат фокуса; `aria-modal="true"` честен. БЕЗ `inert`/`aria-hidden` фона (компенсировано aria-modal; inert — 3.8/полировка). ~~**Полное управление фокусом диалога методики** (`web/src/features/flag/MethodologyDialog.tsx`) — возврат фокуса на триггер при закрытии, надёжный Escape (document-level listener вместо keydown на узле диалога), focus-trap + `inert`/`aria-hidden` фона. Сейчас `aria-modal="false"` честно отражает отсутствие трапа; Escape работает пока фокус внутри. Тот же паттерн в `MapPreviewSheet` (1.8). Полное решение — Epic 3 Story 3.5.~~
- **`formatMoney` роняет карточку на нечисловой непустой evidence-строке** (`web/src/features/flag/MethodologyDialog.tsx`) — truthy-гард ловит пустую строку, но `formatMoney('38 400 000')` бросит (regex `^-?\d+$`), а диалог — child без error-boundary → падает вся карточка. Сейчас evidence ручные и каноничны (`'38400000'`). Когда evidence станут динамическими (Epic 4 compute) — обернуть в безопасное форматирование с честной деградацией (не crash).
- **Дефолты фактора/выборки фабрикуют значения при неполных данных** (`MethodologyDialog.tsx`) — `factor ?? '1.5'`, `sampleSize ?? 0`, `minSample ?? 5` молча подставляют значения, если поля evidence отсутствуют, что нарушает «воспроизводимость вручную» (число в прозе ≠ реальному). Сейчас данные полны. При динамических evidence (Epic 4) — требовать непустые поля или честно скрывать (как truthy-гарды raised-блока).
- **Нет структурного теста паритета ключей i18n kk↔ru** — `i18n.test.ts` проверяет покрытие honest-states, но не равенство наборов ключей `methodology.*`/`flag.*`/`report_error.*`. При потере ключа в kk — молчаливый ru-фолбэк (`fallbackLng:'ru'`), скрытая регрессия для kk. Уже отмечено в 1.6 deferred (двунаправленная сверка). Добавить при росте локалей.

## Deferred from: code review of 1-8-скелет-карта-с-pmtiles-подложкой-и-тремя-точками (2026-06-23)

- **✅ РЕШЕНО Story 3.5 (2026-07-03):** предшественник `MapPreviewSheet`/`LotPreviewSheet` удалён; новый `PreviewSheet` несёт полный trap + возврат на триггер-маркер (фолбэк — контейнер карты при рекластеризации). ~~**Focus-return при закрытии превью-листа** (`web/src/features/map/MapPreviewSheet.tsx`) — лист `role=dialog` ставит фокус на себя при открытии, но при закрытии (Escape/кнопка/бэкдроп) фокус не возвращается на маркер-триггер (теряется на `<body>`). Полное управление фокусом (focus-trap + return + `inert` фона) отложено в Epic 3 Story 3.5. Минимум на будущее: сохранить триггер-элемент и вернуть фокус на `selected→null`.~~
- **Canvas-фон и `prefers-reduced-motion` — снимок на момент инициализации** (`web/src/features/map/MapView.tsx`, `shared/map/tokenBridge.ts`) — фон карты читается из токена один раз при `new Map()`, `PREFERS_REDUCED_MOTION` — на уровне модуля. Не реагируют на рантайм-смену темы (через `setTheme`) или системной настройки движения. Сейчас рантайм-переключателя тем в UI нет → не проявляется. Когда появится: `MutationObserver` на `[data-theme]` → `map.setPaintProperty('aq-neutral-bg','background-color', …)`; `matchMedia(...).addEventListener('change')` для reduced-motion.
- **Декоративные `rgba()`-цвета в `map.css` вне токен-системы** — тень пина `rgba(12,26,34,.30)` (значение из DESIGN.md), scrim бэкдропа `rgba(12,26,34,.4)`, тень листа. Проходят stylelint (`color-no-hex` намеренно разрешает функциональную нотацию), но не следуют тёмной теме. Прямое добавление rgba-токенов рискует сломать `contrast.test.ts` (парсит hex). Решение — ввести в дизайн-систему (Story 1.5-домен) семантические токены elevation/scrim (composite/alpha) и заменить литералы.
- **✅ РЕШЕНО Story 3.5 (2026-07-03):** превью различает `status===404` → честное «нет данных по объекту» (`contract.not_found`), прочее → `contract.error`; e2e-сценарий 404 закреплён. ~~**404 по несидированному контракту = общая ошибка, не «нет данных»** (`web/src/features/map/MapPreviewSheet.tsx`) — превью точки `DEMO-0002`/`0003` (нет в seed) показывает `contract.error` («не удалось загрузить»), а не честное «нет данных по объекту». Унаследовано от `ContractRoute` (1.7). Реальные данные/seed трёх контрактов-историй — Story 1.9; тогда же различить `status===404` → честное «нет данных».~~

## Deferred from: code review of 1-7-сквозная-карточка-контракта-walking-skeleton-dod (2026-06-22)

- **`caddy` зависит от `api` только по старту, не по health** (`deploy/docker-compose.yml`) — `caddy.depends_on: [api]` (короткая форма = `service_started`), а у `api` нет `healthcheck` (образ distroless — нет shell/curl для стандартной проверки). Первые запросы `/api/*` сразу после `docker compose --profile app up` могут получить 502 от `reverse_proxy api:8080`, пока chi поднимается. Для ручной DoD-проверки — транзиентный флай. Захардить позже: добавить Go-based healthcheck в api (вызов своего бинаря) + `caddy.depends_on.api.condition: service_healthy`, ИЛИ `lb_try_duration`/retry в Caddyfile reverse_proxy.
- **`migrate` тянет goose из сети на каждом холодном старте** (`deploy/docker-compose.yml`) — `go run github.com/pressly/goose/v3/cmd/goose@v3.27.1` скачивает модуль при каждом запуске one-shot `migrate`, без ретрая. Хрупко к сетевым/IPv6 сбоям среды (см. memory: Docker Hub/IPv6 ретраи). Захардить: предсобрать goose в builder-слое образа / вендорить / добавить `restart`-политику с бэкоффом для one-shot миграции.

## Deferred from: code review of 1-1-монорепо-скелет-и-compose-db (2026-06-21)

- **`/metrics` на публичном listener без гейтинга** (Story 1.3) — `cmd/api` отдаёт `/metrics` (promhttp: go-runtime-внутренности, позже SM-C1/SM-C2) на том же публичном listener, что и `/api/*`. В **Story 1.7** (Caddy reverse-proxy) ограничить путь `/metrics` (allow только внутренние IP) ИЛИ вынести на отдельный admin-listener.
- **`amount_tng` — целые тенге (решение владельца, Story 1.3)** — в **Epic 2** (живой импорт) импортёр обязан: (а) валидировать/округлять дробные суммы goszakup, если такие встречаются; (б) guard на overflow BIGINT (`>9.2e18`). Сейчас S-0/синтетика — целые тенге.
- **sqlc-регенерация зависит от распознавания goose `-- +goose Down`** (Story 1.2) — `server/sqlc.yaml` читает `../migrations` целиком; sqlc v1.31 корректно игнорирует Down-секции (проверено: генерация чистая), но это version-хрупкое неявное допущение. В **Story 1.4** добавить CI-страж «generated==regenerated» (уже в плане `ci-registry.yml`), чтобы дрейф sqlc/миграций ловился автоматически.
- **Скомпилированный бинарь `stage0-audit/stage0-audit` отслеживается git** — предсуществующая проблема (не вызвана Story 1.1). Бинарь ~9.6 МБ закоммичен в ранних коммитах; меняется при каждой сборке `stage0-audit`. Вычистить в рамках Story 0.2: `git rm --cached stage0-audit/stage0-audit` + добавить правило в `.gitignore` (например `stage0-audit/stage0-audit`).

## Deferred from: code review of 1-6-i18n-каркас-kz-дефолт-и-обёртки-инкапсуляторы (2026-06-22)

- **i18n init-порядок безопасен только на in-memory ресурсах.** Сейчас `i18n.init({resources})` синхронен → `App` рендерится после готовности, `<Suspense>` не нужен. При добавлении async-backend (`i18next-http-backend`/lazy-namespaces) `useTranslation` начнёт suspend'иться, а `<Suspense>`-границы нет → пустой рендер/ошибка. Зафиксировать инвариант или добавить `<Suspense>` при переходе на async-загрузку локалей.
- **i18n-cross тест однонаправленный.** Проверяет «локаль покрывает каждый honest-state», но не ловит ЛИШНИЕ ключи и даёт `TypeError` (не чистый assert) при отсутствии объекта `value_state`/`flag_state` в локали. При росте локалей — сделать двунаправленную сверку (равенство множеств) + мягкий fail.
- **DOM-render тесты обёрток** (`<LangValue>`/`<DataState>`/`<Icon>`) — требуют RTL+jsdom (новые devDeps). Отложено на Playwright-резерв (`ci-web.yml`) ИЛИ добавить по одобрению владельца. Сейчас покрыто чистой логикой + tsc.

## Deferred from: code review of 1-5-дизайн-токены-и-две-темы (2026-06-22)

- **Codegen `tokens-codegen.mjs` — устойчивость к НЕштатному `tokens.json`.** Сейчас вход авторский (не внешний), триггеров нет. При росте/внешнем источнике добавить: ошибку (не молчаливую потерю) для узла с `$value` + дочерними; экранирование `}`/`;`/переводов строки в значениях; детекцию дублей CSS-var-имён из разных путей; экранирование кавычек в TS-union; явную сортировку ключей (сейчас порядок numeric-ключей делегирован V8 — вывод детерминирован, но порядок source≠output для integer-ключей может удивить редактора).
- **Фронт-тулинг латентные дыры.** `lint:css --allow-empty-input` вернёт зелёный, если glob промахнётся мимо будущего фичевого CSS (страж молча перестанет защищать) — добавить проверку, что glob матчит ожидаемое, когда появится фичевый CSS. `scripts/` не входит в tsconfig → `tokens-codegen.d.mts` может разойтись с `.mjs` без ошибки компиляции; при росте codegen — перенести на TS или добавить тест соответствия типов.

## Deferred from: code review of 1-4-registry-двухосевой-enum-честных-состояний-render-каркас (2026-06-22)

- **Матчер нейтральности однонаправленный** (`server/internal/registry/neutrality.go`) — `FindTaboo` проверяет лишь ОТСУТСТВИЕ taboo-строк, но не ПРИСУТСТВИЕ обязательной нейтральной рамки в выводе. Архитектура (doc-нейтральность) предполагает двунаправленный матчер. Сейчас рамка `frame.signal` структурно гарантирована в `render.Render`, поэтому риск низкий. Активировать в **Epic 4**, когда появятся per-flag шаблоны прозы (текст уже не сводится к одной рамке).

## Deferred from: code review of 0-8-ранняя-карта-астаны-на-реальных-лотах (2026-06-24)

- **`lots.amount BIGINT` без `CHECK (amount >= 0)`** (`server/.../migrations/0003_projection_lots.sql`) — отрицательная сумма лота (ошибка парсинга scrape-источника) проходит `fromInt8String` → `ok`-envelope → `formatMoney` рендерит «-5 000 000 ₸» как валидную сумму (фабрикация факта вопреки гардрейлу честности). Pre-existing (схема Story 2.0), не введено рефактором 0.8. Активировать при упрочнении импорта (Epic 2): добавить `CHECK (amount >= 0)` ИЛИ трактовать `amount < 0` как честное состояние (`no_data`/`not_comparable`), не как валидное значение.
- **✅ РЕШЕНО Story 3.5 (2026-07-03):** `LotPreviewSheet` удалён (D8); все слои (лист/методика/форма/fallback) — `useDialogFocus` + честный `aria-modal="true"`. ~~**`aria-modal="false"` на `role="dialog"` + нет focus-trap** (`web/src/features/map/LotPreviewSheet.tsx`) — лист превью визуально модален (затемнение, закрытие по фону/Escape), но для скринридера объявлен немодальным; фокус не заперт. Активировать в **Epic 3** (Story 3.8 «доступность canvas-карты a11y») вместе с полным focus-trap.~~

## Deferred from: code review of story-4.3 (2026-06-25)

- **Инвариант провайдера флагов: delta-семантика (решение владельца 2026-06-25).** Пересчёт (`applyContractFlags` + `Recompute*`) — delta: трогает только контракты из входного батча; выпавший из набора контракт сохраняет `is_active=true`, пустой батч — no-op. На синтетике безопасно. В **Epic 2** живой провайдер (`/v2/contract`, `/v2/trd-buy`) ОБЯЗАН передавать каждый контракт, чьё состояние мог изменить (включая «состояние ушло» → передать с not-raised для авто-снятия). Если универсум когда-либо сжимается (контракты удаляются из источника), отдельно решить снятие «осиротевших» активных флагов. Затрагивает общий хелпер 4.2 + 4.3.
- **int64-переполнение в сравнении цены/км** (`server/internal/flags/price_per_km.go:56`) — `*price×1000 > *median×1500` переполняет int64 при `median > ~6.1e15` ₸/км и молча инвертирует вердикт. Теоретически реально, но недостижимо в домене (≈60× ВВП РК на километр; деньги — целые ₸). Накрывается тем же honest-guard'ом, что и непозитивная медиана (patch-находка), если решим расширить. Активировать при упрочнении импорта живых сумм (**Epic 2**) вместе с guard на overflow BIGINT.
- **factorScale=1000 усекает deviation_factor с >3 знаками после запятой** (`price_per_km.go:23,55`) — порог `round(factor×1000)`; для v1 (1.5 → 1500) точно, но фактор вроде `1.2345` молча усечётся, и коммент «бит-в-бит» переоценит детерминизм. При вводе НЕ-1.5 фактора (**4.6 / 2.4**, наполнение methodology_params DB) добавить валидацию `factor×scale` целым в `methodology.Load` ИЛИ перейти на рациональ `numer/denom`.
- **Чтение медиан вне транзакции записи флагов** (`risk_flags.go:178-188`) — `RecomputePricePerKM` зовёт `bench.MedianForKey` по-входно (отдельные авто-commit чтения), затем пишет флаги в своей транзакции. Конкурентный `BenchmarkStore.ReplaceSnapshot` в середине батча мог бы дать флагам в одном пересчёте разные снапшоты медиан. Сейчас безопасно (atomic swap + предполагается одиночный recalc-джоб). При живом recalc-пайплайне (**Epic 2**) зафиксировать одиночность джоба ИЛИ читать все медианы в одном снапшоте/транзакции с записью.
- **severity всегда NULL** (`risk_flags.go:60-65`, `applyContractFlags`) — `RaiseContractFlagParams.Severity` не задаётся → колонка NULL для ВСЕХ флагов (тот же паттерн, что в Story 4.2). Ни один AC 4.2/4.3 severity не требует; NULL честен. Активировать при появлении фильтрации/сортировки по severity (presentation **Epic 5** / поиск **Epic 6**) — решить, откуда берётся severity (методика? фиксированная?).
- **int32-усечение `sample_size`** (`server/internal/store/projection/price_benchmarks.go`) — pre-existing Story 4.1; `int32(r.SampleSize)` усекает при `> MaxInt32`. Вне диапазона Astana-пилота. Активировать при национальном масштабе вместе с guard.
- **Опциональное упрочнение тестов 4.3** — golden-фикстура не содержит ±1 у границы порога (юнит-тест `TestPricePerKM_States` содержит `1_500_001`/`1_499_999`); `TestPricePerKM_Evidence` проверяет evidence только на `raised`, не на `insufficient_data`-пути (evidence заполняется ДО early-return, регрессия-обнуление не поймалась бы). Низкий приоритет: negative-control + юнит ±1 уже дают сильное покрытие.

## Deferred from: code review of story-4.4 (2026-06-25)

- **int64-переполнение в сравнении доли монополии** (`server/internal/flags/monopoly.go:222`) — `*supplier×1000 >= *total×500` переполняет int64 при `group_total_sum > ~1.8e16` ₸ (групповой агрегат; ≈90× ВВП РК — недостижимо в домене). Честный guard на непозитивный знаменатель уже стоит. Активировать при упрочнении импорта живых сумм (**Epic 2**) вместе с guard на overflow BIGINT (как 4.3).
- **Инвариант провайдера монополии: без дублей субъекта в батче** (`RecomputeMonopoly`) — при двух `MonopolyInput` с одним `OrganizationID` в батче UPSERT срабатывает дважды в транзакции (last-wins). DELTA-провайдер (решение 2026-06-25) обязан давать уникальные субъекты. На синтетике не возникает. В **Epic 2** живой провайдер (`SUM` по БИН/группе) обязан дедуплицировать по organization_id ИЛИ задокументировать last-wins.
- **Нетипизированный `flagOutcome.subjectID`** (`store/projection/risk_flags.go`) — несёт `contract_id` (4.2/4.3) ИЛИ `organization_id` (4.4) по контексту вызова, без type-тега. Текущая разводка корректна; рантайм-сеть — CHECK `risk_flags_subject_ref_chk` (поймает org_id в contract-флаге). Рассмотреть типизированный `subjectKind{kind, id}` при росте числа субъект-типов — hardening.
- **`clear` шлёт zero-row UPDATE для never-flagged not_raised субъектов** (`applyFlagOutcomes`) — корректно (idempotent), но на масштабе батча — лишние UPDATE, матчащие 0 строк. При живом провайдере (**Epic 2**) рассмотреть: clear только при наличии активного флага (но это +1 чтение на субъект — взвесить).
- **`shareScale=1000` усекает concentration_share с >3 знаками после запятой** (`monopoly.go:30`) — для v1 (0.5) точно; порог вроде `0.6667` усечётся. При вводе НЕ-0.5 порога (**4.6 / 2.4**) добавить валидацию `share×scale` целым в `methodology.Load` ИЛИ рациональ `numer/denom` (как factorScale 4.3).

## Deferred from: code review of story-4.5 (2026-06-25)

- **`dateToUnix` игнорирует `pgtype.Date.InfinityModifier`** (`server/internal/store/projection/risk_flags.go`) — ±infinity-дата (Valid=true) даёт мусорный epoch вместо honest nil. При живом /v2/rnu-импорте (**Epic 2**) добавить `if d.InfinityModifier != pgtype.Finite { return nil }`.
- **Гранулярность дат РНУ: DATE (полночь UTC) vs `clock.Real`** (`rnu.go`/`dateToUnix`) — `end_date=сегодня` снимается в полдень дня истечения при живых часах; «истекает сегодня» неоднозначно (off-by-до-24ч). На синтетике (`clock.Fixed` в полночь) консистентно. При **Epic 2** (Real-clock) решить date-granular сравнение — truncate `now` к UTC-полночи перед сравнением с DATE-границами.
- **`now` перечитывается per-record, не снапшотится на батч** (`RecomputeRNU`/`rnu.go`) — длинный батч с живыми часами мог бы пересечь границу суток (ранние записи против иного `now`, чем поздние). При **Epic 2** снапшотить один `now` на весь пересчёт.
- **CHECK `end_date >= start_date` допускает `start==end`** (`migrations/0008_rnu_entries.sql`) — однодневная запись (`start==end==today`): start≤now И end≤now → not_raised в день старта (инертна). Решить семантику нулевой-длительности при **Epic 2**.
- **Store collapse not_published/insufficient → clear** (`RecomputeRNU`) — kill-switch-off и битая запись сводятся к снятию флага, как обычный not_raised; различие теряется на границе store. Пересмотреть при presentation (**Epic 5**), если рендеру нужно отличать not_published от not_raised.
- **Negative-control загрузчика не доказывает причину** (`methodology_test.go`) — кейсы `«…enabled отсутствует»` проверяют `err != nil`, не подстроку ошибки ([[guards-must-prove-red]]). Усилить assert'ом причины для обоих enabled-флагов (single_participant + rnu).
- **Инвариант импортёра /v2/rnu (Epic 2): НЕ удалять записи при истечении** — проставлять `end_date` (delta-пересчёт `RecomputeRNU` не посещает удалённые строки → устаревший активный флаг остался бы). Зафиксировать в контракте импортёра (комментарий 0008 + Dev Notes уже фиксируют модель «end_date, не DELETE»).

## Deferred from: code review of story 2.3 (2026-06-27)

- **Conflict фиксирует только первый БИН** (`orgnorm/plan.go` дедуп по `(raw_name, source)`) — при «одно имя → N БИН» создаётся одна conflict-строка, привязанная к первому; прочие вовлечённые орг не получают alias. Conflict name-keyed (корректно для модели); полное per-БИН вовлечение реконструируется на чтении в **Story 5.2**.
- **Синоним-фолдинг только whole-string/word** (`normalize.CanonicalKey`) — multi-word орг-форма-префикс («Товарищество с ограниченной ответственностью «X»») не сворачивается к «тоо x» (фразовый синоним матчит лишь целую строку). Core-варианты (casefold/гомоглифы/пунктуация/однословные синонимы) работают. Substring-фолдинг орг-форм — лексикон-улучшение (новая версия лексикона).
- **orgBool/orgString строгие type-assert** (`decode/organizations.go`) — числовой `participant_bin`/`"1"`-winner на живом ows → молча `""`/`false` (БИН теряется, победитель не подрядчик). Тот же класс, что value-robustness-defer Story 2.0 → **Story 2.1/2.2** (живой decode): принимать `json.Number`/строковые числа/булевы варианты.
- **Apply не транзакционен** (`orgnorm/apply.go`) — org-upserts и alias-upserts раздельными `Exec` поверх пула; сбой в середине оставит орг без псевдонимов. Идемпотентно (ретрай чинит). Транзакция/атомарность импорта → **Story 2.4/2.6**.
- **UpsertOrganization проекционные нюансы** (`queries/organizations.sql`) — (a) `name_ru = COALESCE(EXCLUDED, …)` = last-import-wins: отображаемое имя «мигает» по порядку батчей; (b) `is_deleted` не сбрасывается на DO UPDATE → реанимация soft-deleted орг скрыта; (c) `first_seen_at`/`source_url` никогда не заполняются (всегда NULL). Живое наполнение + политика → **Story 2.2**.
- **alias FK на surrogate `organizations.id`** (`migrations/0013`) — truncate-rebuild проекции (`RESTART IDENTITY`) оборвёт `organization_id` кураторских строк. Безопасно под UPSERT (реальный механизм импортёра; id стабильны). Полная rebuild-устойчивость (rebind по БИН) → **Story 2.6** (S-0-приёмка «правка переживает ре-импорт» на реальных 2 импортах).
- **CanonicalKey пунктуация-пробелы** (`normalize.CanonicalKey`) — не делит `()`, `/`, кавычки-ёлочки `„""`, апостроф → «ТОО Вода (Астана)» ≠ «ТОО Вода Астана». Направление безопасное (under-merge → отдельные/manual, не ложное слияние). Расширить разделители при тюнинге лексикона.

## Deferred from: dev-story 5.2 (2026-06-27)

- **Живая ссылка «Подрядчик» из карточки контракта → `/contractors/{bin}`** (закрытие defer 5-1) — требует выставить `supplier_bin` в контракт-DTO (join `organizations` по `supplier_org_id`). Прямой джойн в `GetContractByID` ломает тип (sqlc: SELECT перестаёт покрывать таблицу → Row-тип), поэтому нужен отдельный лёгкий запрос supplier-орг + поле DTO + OpenAPI + ContractCard-ссылка + контракт-тесты. Карточка подрядчика достижима по URL; кросс-ссылка — мелкий follow-up (наполнение supplier_org_id всё равно ждёт Story 2.2 — до неё ссылка была бы только на seed-связанных контрактах).

## Deferred from: code review of story 5.2 (2026-06-27)

- **tz day-boundary РНУ** (`httpapi/contractors.go resolveRNUMarks`) — `asOf.UTC().Truncate(24h)` сравнивает UTC-календарные дни; платформенный день — Астана (UTC+5/6). У полуночи метка может «мигнуть» ±1 день. Дата-колонки без tz (день-гранулярно). Снапшот единого `now`/tz при Epic 2.
- **formatMoney точность >2^53** (`features/contractor/ContractorCard.tsx`) — деньги на проводе строкой (2^53-safe), но `formatMoney` парсит через Number → суммы >~9e15 ₸ теряют точность. Для Астана-пилота недостижимо.
- **source_url без allowlist схемы** (`ContractorCard.tsx`) — `href` из данных без `https?:`-проверки; данные из офиц. импорта/оператора (риск низкий). Добавить allowlist при недоверенных источниках.
- **`ListContractorFlags` без `subject_type='contractor'`** (`risk_flags.sql`) — partial-unique `(flag_type, organization_id)` гарантирует 1 строку/тип; фильтр defensive-only.
- **unverified показывает агрегаты** (`contractors.go`) — при «профиль уточняется» флаги insufficient, но count/sum/regions всё ещё фактичны. Возможно by design (связи=факт, флаги=вывод о подрядчике); пересмотреть семантику при живой линковке Story 2.2.
- **РНУ-метка с будущим start_date → active** (`resolveRNUMarks`) — реконструкция активности игнорирует `start_date`; запись «ещё не вступила» показалась бы active. Маловероятно в реальном РНУ.
- **«недобросовестный»-цитата структурно не загейчена** (`contractorCardStrings.test.ts`) — нейтральность-тест ловит только taboo-корни; вынос слова из `contractor.rnu.registry_name` не поймался бы. Усилить assert «слово только в registry_name».
- **методика-tap-through на флагах подрядчика** — карточка контракта (5-1) открывала методику по клику на non-raised флаг; карточка подрядчика проще (monopoly/rnu methodology-обвязка не подключена). Отдельный follow-up.

## Story 2.4 — отложенное

- **✅ РЕШЕНО 2026-07-01 (option (b) — раздельные seed/clean прогоны):** `make test-integration` разделён на **`test-integration-clean`** (`./internal/ingest/... ./internal/store/projection/... ./internal/outbox/...` — ЧИСТАЯ мигрированная БД) и **`test-integration-seed`** (`./internal/store ./internal/httpapi/...` — migrate+`db-seed`); CI `ci-server.yml` получил второй job **`integration-seed`** (свежий postgis+migrate+seed → SEED-группа), дополняющий существующий `integration` (CLEAN-группа) — теперь ОБЕ группы = гейты merge. **Верифицировано на реальном Postgres (55432, миграции 0001-0019):** двунаправленная несовместимость воспроизведена (CLEAN на seed→FAIL `TestRNUFlag_*` глобальный счёт; SEED на clean→FAIL `TestGetContractByID DEMO-0001`), обе группы зелёные каждая на своём состоянии. Выбрана option (b) (сплит), НЕ (a) (пер-тестовая самоочистка) — ниже churn, не трогает 22 тест-файла. ~~**Изоляция integration-тестов: clean vs seed несовместимы** — projection (Epic 4: `Count*Active` глобально) и orgnorm/pipeline ожидают ЧИСТУЮ мигрированную БД; store/httpapi (Epic 5.1/5.2: DEMO-0001) требуют `make db-seed`. `make test-integration` против одного состояния даёт часть провалов. Фикс: пер-тестовая очистка ИЛИ раздельные seed/clean прогоны в CI.~~ (surfaced 2026-06-29, закрыт 2026-07-01)
- **Живой импорт `/v2/journal` + живые провайдеры флагов (single/price/monopoly) + нормализация-над-источником → Story 2.2** — `cmd/importer` проводит постимпортный конвейер с ПУСТЫМИ провайдерами этих флагов (no-op) и без нормализатора; РНУ-провайдер живой (`ListRNUInputs`). Подключить реальные провайдеры + Source→decode→UPSERT при живом импорте (токен).
- **Median single-snapshot при живом price-провайдере (Story 2.2)** — долг deferred-work «median reads вне flag-write tx» закрыт на 2.4 инвариантом одиночного джоба (`RunPostImport` — единственный постимпортный джоб, benchmark swap ПЕРЕД flags) + пустым price-провайдером. При подключении живого price-провайдера в 2.2 подтвердить: либо одиночность джоба гарантирована оркестрацией, либо читать все медианы в одном снапшоте/транзакции (AR-9).
- **Глобальный snapshot_id-штамп/указатель (AR-5, при пилоте)** — 2.4 публикует лёгкий снапшот через per-table транзакционный swap (MVCC), без глобальной таблицы-указателя/version. Тяжёлый иммутабельный снапшот-стор отложен архитектурой до пилота.

## Deferred from: code review of story-2.4 (2026-06-29)

- **Advisory-lock enforcement одиночности постимпортного джоба → Story 2.6** — `RunPostImport`/`cmd/importer` декларируют «единственный джоб, не запускать конкурентно» только КОММЕНТАРИЕМ; нет `pg_advisory_lock`/mutex. Долг deferred-work:201 (флаг против устаревшего benchmark) смягчён конвенцией + пустым price-провайдером (moot до 2.2), но реальное enforcement под конкуренцией = идемпотентность Story 2.6. Два процесса importer (cron+ручной) могли бы дать флаги против чужого снапшота.
- **✅ РЕШЕНО 2026-07-01 — см. запись «Изоляция integration-тестов» в разделе Story 2.4 выше.** Разделено на `test-integration-clean`/`test-integration-seed` + CI-job `integration-seed`; верифицировано на реальном Postgres. ~~`make test-integration` несовместимость clean-vs-seed: таргет гоняет `./...`, но projection/orgnorm/pipeline требуют ЧИСТУЮ БД, store/httpapi — `make db-seed`; таргет не мог быть pass/fail-гейтом.~~

## Deferred from: code review of story-2.6 (2026-06-29)

- **Полная разводка грантов projection⊥curation под role-DSN деплой → ops / Story 2.2** — миграция 0015 покрывает НАЗВАННЫЙ в AC набор таблиц, но для реального подключения importer/Directus РАЗНЫМИ ролями нужны: гранты на оставшиеся таблицы (flag_disputes/error_reports — write API; methodology_params — config seed; acts/interim_geo_lots — проекция importer), GRANT membership роли подключающему юзеру (иначе SET ROLE требует superuser), LOGIN/пароль ролям. Сейчас app коннектится owner-ролью (гранты инертны) — корректность проверяется только стражем границы. Завершить при wiring role-DSN (Story 2.2).
- **✅ РЕШЕНО 2026-07-01 (option (b), НЕ ретрофит самоизоляции) — см. запись «Изоляция integration-тестов» в Story 2.4 выше.** Вместо переписывания 22 тест-файлов под самоизоляцию выбран сплит: `test-integration-clean` (projection глобальные счётчики на чистой БД) / `test-integration-seed` (store на seed) + CI-job `integration-seed`. Подтверждено: `TestRNUFlag_*` глобальный счёт краснеет на seed → CLEAN-группа изолирована на чистой БД. ~~Ретрофит Epic-4 projection-тестов под самоизоляцию: `CountActive` глобально → краснеет на засеянной БД. Фикс: самоочистка ИЛИ раздельные прогоны.~~
- **S-0 fault-injection negative-control** — assert'ы R-intact/no-dupes/median-ε=0 субстантивны на реальных данных, но без инъекции дефекта (снять auto-гейт / вставить дубль), доказывающей ПРИЧИНУ покраснения per §4. Митигировано sibling-тестом; полноценный fault-inject — follow-up.

## Deferred from: code review of story-2.7 (2026-06-29)

- **✅ РЕШЕНО Story 7.1 2026-06-30:** введена политика max-attempts + dead-letter (migration `0019_outbox_dead_letter`: `dead_at`/`dead_reason` + частичный индекс `WHERE sent_at IS NULL AND dead_at IS NULL`; `outbox.ProcessBatchN(maxAttempts)` + чистый `shouldDeadLetter`; poison-строка при потолке помечается dead и выпадает из поллинга). `ProcessBatch` = обёртка с `DefaultMaxAttempts=10`. ~~**Dead-letter / max-attempts для outbox → Epic 7** — `ProcessBatch` растит `attempts` бесконечно: poison-строка (вечно падающий `Send`) ре-поллится каждые `backoffMax`(30m) навсегда, без квартина/алерта; `attempts` (int32) теоретически переполняется.~~
- **`Dispatcher.Send` под FOR UPDATE-локом всего батча (lock-across-IO) → Epic 7** — `ProcessBatch` держит row-локи всех опрошенных строк + pooled-conn на время `Send` каждой строки (одна tx на батч). Для S-0 (LoggingDispatcher мгновенен) и по AC2 это корректно, но сетевой Telegram-диспетчер Epic 7 будет держать локи/коннект через сетевой вызов. Реструктурировать: claim+commit короткой tx → доставка вне tx → mark-sent отдельной tx.
- **At-least-once amplification: откат батча передоставляет уже-Send-нутое → Epic 7** — поздняя DB-ошибка (MarkSent/BumpAttempt/Commit) на строке k откатывает весь батч; строки 1..k-1 уже физически доставлены (`Send`), но их `sent_at` откатан → передоставка на следующем проходе. Outbox by-design at-least-once (дедуп у получателя по `event_id` — Epic 7); ограничить амплификацию (per-row commit) ИЛИ полагаться на receiver-dedup. В S-0 безвреден (LoggingDispatcher лишь логирует).
- **`NewEventID()` без/с пустыми частями → коллизия id → Epic 7** — `NewEventID()` (без аргументов) и `NewEventID("")` хэшируют пустую строку → один и тот же не-нулевой id → разные логические события склеиваются (`ON CONFLICT DO NOTHING` тихо роняет все кроме первого); `IsZero()` это не ловит. В S-0 вызывается только тестами со стабильными частями. При появлении реальных эмиттеров (Epic 7) — обязать непустой стабильный дискриминатор (гард/паника на пустой вход).
- **Батч-старт `now` переиспользован для retry-scheduling + `sent_at` (повторное ревью 2.7, 2026-06-29) → Epic 7** — `ProcessBatch` берёт `now := clk.Now()` один раз на старте батча и пишет им `available_at = now + backoff(...)` и `SentAt = now`. При долгих/медленных батчах (сетевой диспетчер Epic 7) `next` может оказаться в прошлом относительно реального времени → плотный ретрай без реальной паузы (эрозия O-4 backoff); `sent_at` предшествует фактической доставке (мелкая нечестность таймстампа). В S-0 (LoggingDispatcher мгновенен) moot. Бандлить с реструктуризацией lock-across-IO (выше): брать свежий `clk.Now()` в момент mark/bump.
- **`SubjectURN` с пустыми частями проходит валидацию непустоты (повторное ревью 2.7, 2026-06-29) → Epic 7** — `SubjectURN("contract","")→"contract:"` непуст, поэтому проверка `SubjectRef==""` в `Enqueue` его пропускает → в БД попадает URN без public_id. Та же семья, что коллизия `NewEventID` (выше); закрыть тем же Epic-7-гардом: требовать две непустые части URN. Эмиттер-контролируемо, в S-0 эмиттеров нет.

## Deferred from: dev-story 6.1 (2026-06-29) — ПРЕД-СУЩЕСТВУЮЩИЙ долг (НЕ регресс 6.1)

- **✅ РЕШЕНО 2026-06-30 (commit `2acb80f`, ретро Epic 6 AI-1b):** root cause = дрейф фикстуры ↔ Contract-схема — `ContractRoute` (5.6/AR-29) читает поля верхнего уровня `as_of`/`methodology_version`/`methodology_drift`, которых не было в DEMO-фикстурах (DEMO-0001/0002/0003) → `c.as_of` undefined → throw → RouteError. Фикс: добавлены 3 поля в фикстуры. Полный playwright 23/23 зелёный. ~~**`web/e2e/contract.spec.ts` + `web/e2e/flag.spec.ts` падают на HEAD `87cb595` → Epic 5 follow-up** — карточка контракта в e2e рендерит RouteError («Бірдеңе дұрыс болмады») вместо данных: `getByTestId('contract-amount')` не находится, форма «Сообщить об ошибке» недостижима (7 тестов).~~ **Доказано baseline-прогоном:** `git stash -u` (убрать всю работу 6.1/2.7) → `npx playwright test e2e/contract.spec.ts` падает ИДЕНТИЧНО на чистом HEAD. Значит долг ПРЕД-СУЩЕСТВУЮЩИЙ — web-e2e не гонялся в backend-only сессиях 2.4/2.6/2.7, регресс карточки (Epic 5) всплыл только сейчас. Story 6.1 НЕ затрагивает карточку контракта (свой роут `/search`; `e2e/search.spec.ts` 3/3 зелёные). Нужно: отдельно диагностировать причину RouteError карточки (вероятно дрейф формы DEMO-мока ↔ Contract-схема ИЛИ рантайм-throw в ContractCard на KK-пути) и починить — вне скоупа 6.1. ⚠️ В CI `ci-web.yml` (Playwright) этот долг = красный гейт; чинить до merge web-изменений.

## Deferred from: code review of 6-2-поиск-по-бин-и-наименованию (2026-06-30)

- **stale-`sp` race в 6.1 amount-debounce (`BrowseContracts`) → Epic 6 follow-up / Story 6.1 долг** — debounce-эффект суммы (`useEffect(..., [amountMin, amountMax])` в `SearchRoute.tsx`/`BrowseContracts`) захватывает `sp`/`filters` из рендера; тоггл другого фасета в окне 350мс → таймер не сбрасывается → фиксит на устаревших params → затирает только что применённый фасет. ПРЕД-СУЩЕСТВУЮЩИЙ паттерн из Story 6.1 (тот же класс, что фикс P4 в 6.2 для `q`-debounce). Вне scope 6.2; чинить тем же приёмом (функциональный `setSp(prev=>…)`) при касании 6.1-кода.

## Deferred from: dev-story 6.3 (2026-06-30) — агрегаты района (FR-17)

- **✅ РЕШЕНО 2026-06-30 (commit `21948e9`, ретро Epic 6 AI-1a):** `prettier --write .` отдельным chore-коммитом; `prettier --check .` зелёный. ~~ПРЕД-СУЩЕСТВУЮЩИЙ prettier-долг: `prettier --check .` красный на ~18 закоммиченных файлах (5.1/5.2/6.1) → отдельный cleanup-коммит~~ — `format:check` (CI `ci-web.yml`) флагует `printWidth:100`-нарушения в `ContractorCard.tsx`, `apiFlag.ts`, `useContracts.ts`, `ReportErrorForm.tsx`, `PermalinkButton.tsx`, ряде `*.test.ts` и `filter-chip.css` — НЕ тронуты в 6.3. Доказано: файлы дёрти под пинованной prettier@3.4.2 в их закоммиченном виде (gate был красным до 6.3; web-CI, похоже, не гонялся в backend-сессиях). **6.3-файлы prettier-чисты** (district/*, изменённый ContractCard.tsx, обе chrome.json, e2e/district.spec.ts проверены). Фикс — `prettier --write .` ОТДЕЛЬНЫМ chore-коммитом (не бандлить в историю: churn в 6 чужих фич нарушает чистый per-story commit). Аналог пред-существующего e2e-долга карточки.
- **КАТО-коды районов = null до Story 0.1 (designed seam, не баг)** — `registry/values/astana_districts.json` несёт имена районов, `kato:null` (коды не выдумываем, stage0-access §3; источник — токен-зависимый `/search/getKato`). Пока: страница района агрегирует по КАТО-префиксу из URL, имя честно `no_data` («название уточняется»). Когда 0.1 подтвердит коды — проставить в JSON, БЕЗ изменения кода (имена свяжутся через `NameByKATO` longest-prefix). Владелец выбрал этот вариант (А: имена без кодов).
- **`container_state: not_geocoded` всегда на непустом районе до Epic 3** — у contracts нет гео-колонки (геопривязка — Epic 3, lot-level interim). 6.3 честно эмитит `not_geocoded` при `contract_count>0` («объекты по КАТО, точек на карте пока нет»). Уточнить до фактического счётчика негеопривязанных, когда Epic 3 даст гео контракта. AC1 «негеопривязанные учтены» уже выполнен структурно (агрегация по КАТО без гео-фильтра).
- **Орг-флаги (monopoly/rnu) НЕ агрегируются по району → Epic 6 follow-up / FR-21 district** — `DistrictActiveFlagsByType` джойнит `rf.contract_id = c.id`, поэтому в «Сигналы района» попадают только КОНТРАКТ-субъектные флаги (single_participant, price_per_km). Org-субъектные (monopoly/rnu, `organization_id`, `contract_id` NULL) не считаются. UX-макет рисует «Преобладание подрядчика в районе» — для этого нужен джойн org-флагов → организации, оперирующие в районе. i18n-имя «Преобладание подрядчика в районе» уже добавлено (нейтрально, не «монополия») на случай появления. Отложено (org→district агрегат).
- **Список объектов района bounded 50, без «показать все» → follow-up** — `/api/districts/{kato}` встраивает до 50 объектов (агрегаты считаются по ВСЕМ). Глубокая пагинация / `kato`-фасет на `/api/contracts` для «показать все объекты района» — отложено (пилотный объём терпим).
- **Фасет-чипы и точки входа на странице района → follow-up** — UX-макет рисует фильтр-чипы (Жол/Су/12-24мес) внутри района и вход из карты/полигона; 6.3 отдаёт нефильтрованный агрегат + ссылку с карточки контракта (`kato_code` → `/districts/:kato`). Чипы (переиспользование 6.1) и nav/map-входы — отдельные истории.

## Deferred from: code review of story-6.3 (2026-06-30)

- **`/districts/{kato}` не различает район-уровневый КАТО от город/локалити; `/districts/71` агрегирует весь город, но помечен «район» → резолвится с 0.1** — `district.ValidKATO` проверяет только форму (цифры 2..11); при `kato:null` в реестре нет проверки гранулярности. Запрос префикса `71` матчит все контракты Астаны (`LIKE '71%'`), страница рендерит «Район · КАТО 71». Когда Story 0.1 проставит коды районов — добавить валидацию запрошенного КАТО против реестра (известный район/префикс) ИЛИ маркировку «уровень: город/район/локалити». Сейчас честно (это КАТО-агрегат), но метка «район» неточна для город-префикса.
- **prefix-LIKE индекс (0018) не гарантированно используется под pgx generic-plan → перф на масштабе (NFR-1)** — `kato_code LIKE $1` с `$1=kato+'%'` (bind-параметр): Postgres переписывает в index-range scan только при префиксе-КОНСТАНТЕ на этапе плана. Под кэш-prepared-statements pgx первые прогоны (custom plan) индекс используют, но при переходе на generic plan префикс-оптимизация отпадает → seq-scan. Для пилотного объёма терпимо; на масштабе — рассмотреть `text_pattern_ops`-устойчивый паттерн (literal-префикс / `starts_with` / range `>=kato AND <kato++`). Комментарий миграции смягчён (patch).
- **Три чтения района не snapshot-консистентны** — `DistrictsHandler.Get` шлёт `DistrictAggregates`/`DistrictActiveFlagsByType`/`ListContractsByDistrict` тремя автокоммит-запросами (без общей tx). Конкурентный импорт+atomic-swap между вызовами → `contract_count`/`active_flags_count`/`objects` могут рассогласоваться. Pilot-minor (атомарный импорт 2.4 редок, не на горячем пути). Фикс: обернуть три чтения в одну read-only tx (consistent snapshot) при необходимости.
- **Частичная сумма района показана как полный total при части NULL-сумм** — `total_amount_tng = ok(coalesce(sum))` когда `amount_known_count>0`, даже если у части контрактов сумма NULL → сумма представляет только контракты-с-суммой, без индикации неполноты. КОНСИСТЕНТНО с прецедентом 5.2 `ContractorAggregates` (тот же `coalesce(sum)`-паттерн, принят). Пересмотреть холистически по всем агрегатам (район+подрядчик): показывать ли «частичная сумма (N из M с суммой)» отдельным состоянием. Не регресс 6.3.

## Deferred from: code review of 6-4-медиана-км-района-и-сравнение-с-городом (2026-06-30)

- **Семантика «городской» базы медианы ₸/км → решить при go-live Epic 3** — `medianBlock` (`server/internal/httpapi/districts.go:121,195-220`) безусловно сравнивает префикс района с городским `"71%"` (`cityKato="71"`). Три latent-следствия (сегодня moot — `PricePerKMSamples`→`(nil,false)` → всё `not_comparable`, нет `length_km`): (а) «город `71%`» **включает** контракты самого района → дельта смещена к 0 для района, доминирующего в городе; (б) валидный **не-Астанинский** КАТО (напр. `50…`) сравнивается с медианой Астаны (disjoint-префиксы) без гарда; (в) `/districts/71` = **self-comparison** (district==city → дельта всегда +0%) под меткой «Район». **Решение владельца (2026-06-30): отложить до Epic 3** — методология «что есть город для сравнения» (full-city-incl-district vs rest-of-city) + гарды не-Астанинского/self-случая выбираются на живых данных при появлении `length_km`; форма движка/адаптера/DTO не меняется (прецедент 4.3-шва). Уточняет Open Question **D2** («город = `71`?»). Связано с 6.3-defer про метку «район» для город-префикса (та покрывает лишь label, не методологию медианы).

## Deferred from: code review of 7-1 (2026-06-30)

- **Per-recipient fan-out: transient-сбой одного chat → ре-доставка уже-успешным (дубли) → Story 7.2** — `TelegramDispatcher.Send` (`server/internal/bot/dispatcher.go`) при частичном fan-out (chat A ok, chat B 5xx) возвращает ошибку → воркер `BumpAttempt` → весь EVENT ре-поллится → `Resolver.Recipients` отдаёт `[A,B]` снова → A получает дубль. Дедуп outbox O-3 по `event_id`, не `(event_id,chat_id)`; per-recipient delivery-state нет. Latent (дефолт `NoRecipients`). Закрыть в 7.2 (таблица подписок/доставок): per-recipient идемпотентность (dedup у получателя по `(event_id,chat_id)` ИЛИ таблица доставок + ретрай только не-успевших). Доп: 7.2-`Deactivator` должен быть ИДЕМПОТЕНТЕН (повтор Send на permanent-chat вызовет Deactivate снова; не-идемпотентная ошибка трактуется как transient → цикл до dead-letter). Семья долга 2.7:277 (at-least-once amplification).
- **Throttle.Sleep внутри открытой tx держит FOR UPDATE-локи → live-delivery (бандл с D3)** — `d.Throttle.Wait()` (в `Send`) спит, пока открыта batch-tx воркера (`ProcessBatchN`, `PollUnsent ... FOR UPDATE SKIP LOCKED`) → lock-hold ≈ rows × MinGap. Это проявление УЖЕ отложенного D3 lock-across-IO (deferred-work:276/279): реструктурировать claim→deliver+throttle ВНЕ tx→mark отдельной tx при реальном сетевом транспорте. На mock-транспорте (мгновенном) moot.

## Deferred from: code review of 0-5 (2026-07-01)

- **Апстрим «единственный околоПД» устарел — PRD §7.2 (prd.md:319) + data-model §3 (:92) → скорректировать при следующей ревизии планинг-артефактов.** Обе спеки утверждают, что `telegram_chat_id` — «единственный «околоПД» элемент MVP». Это предшествует `migrations/0011_error_reports.sql:15` (`error_reports.contact TEXT` — опциональный контакт гражданина по FR-28/Story 5.4, тот же §7.2), который является ВТОРЫМ хранимым околоПД-элементом. Story 0.5 (`docs/ops/legal-preflight.md`) исправлена (P1: перечисляет оба + маршрутизирует `error_reports.contact` в OQ-2-style вопрос юристу), но корневая неточность в PRD/data-model — вне scope docs-истории 0.5 (планинг-артефакты). При следующей правке PRD §7.2 / data-model §3 — снять «единственный», учесть `error_reports.contact`. Найдено Edge Case Hunter (code-review 0.5).

## Deferred from: code review of 3-1-автоматическая-геопривязка-batch-nominatim (2026-07-02)

- **✅ РЕШЕНО 2026-07-02 (Story 3.2, миграция 0022):** `verified` добавлен в закрытый enum и терминален для batch — `ListContractsForGeocode` пере-выбирает только `auto`, гейт UPSERT (`IN ('auto','unmatched')`) verified не трогает; доказано `TestVerified_TerminalForBatch` (обе стороны: выпадение из очереди + неприкосновенность для насильного UPSERT). ~~**`auto`-строки geo_objects пере-геокодируются каждый прогон навсегда (нет терминального «verified»-статуса)** — `ListContractsForGeocode` пере-выбирает `g.id IS NULL OR g.geocode_status = 'auto'` без ограничения на возраст/попытки; каждый прогон бьёт по rate-limited Nominatim по всем ранее сматченным контрактам. Закрыть вместе с 3.2.~~
- **`os.Exit()` на путях сбоя настройки в `cmd/geocode/main()` пропускает `defer pool.Close()`/`defer stop()`** — зеркалит установленный прецедент `tools/scrape/cmd/interim-geocode/main.go` (та же форма). Низкий риск для короткоживущего batch-CLI (процесс завершается, ОС освобождает FD/сокеты). Если когда-либо этот паттерн пересматривается для importer/bot/geocode разом — сделать это единообразно, не точечно здесь.
- **Изоляция интеграционных тестов от persistent seed-данных держится на ad hoc «безопасных» ID-префиксах/координатах в комментариях, не на структурном соглашении** (`cmd/geocode/idempotency_integration_test.go`, `internal/store/geo_integration_test.go`, и пред-существующий `internal/store/districts_integration_test.go`) — код-ревью 3.1 поймало на этом реальный баг (координаты/КАТО совпали с seed-районом «Есиль») и пришлось чинить постфактум. Общий шаред-конвенция (например, зарезервированный диапазон синтетических КАТО-кодов/bbox для тестов, задокументированный в одном месте) закрыла бы это структурно. Сквозная проблема тест-инфраструктуры, шире одной истории — не чинить точечно.
- **Миграция `0021_geo_objects_contract_uniq.sql` теоретически упала бы при существующих дублях `contract_id`** — `CREATE UNIQUE INDEX` без предварительного dedup. Эмпирически не воспроизводится на текущих данных (единственный писатель geo_objects до этой истории — идемпотентный `fixtures/seed/geo_objects.sql`, уже `WHERE NOT EXISTS`). Реальный риск нулевой, пока не появится другой писатель geo_objects мимо `UpsertGeoObject`.
- **`nominatim`-сервис в cold compose-профиле без healthcheck — `geocode` мог бы стартовать до готовности self-host импорта** (`deploy/docker-compose.cold.yml`) — `depends_on: nominatim: condition: service_started` гарантирует только «контейнер запущен», не «Nominatim готов принимать запросы» (self-host импорт — 30-90 мин). Нужен проверенный healthcheck-эндпоинт для `mediagis/nominatim:4.4` (не тестировался живьём в этой сессии — писать непроверенный healthcheck рискованнее, чем не писать никакого). Частично смягчено документированной раздельной последовательностью шагов в `docs/ops/geocoding.md` (import → geocode — не единый `up`).
- **`geo_objects.district_id` (Task 3, AC2) не читается ни одним consumer'ом вне нового кода** — проверено grep по `internal/httpapi`, `queries/districts.sql`: ноль ссылок. НЕ баг — AC2-гарантия «негеокодированные учитываются в агрегатах района» уже выполняется через `contracts.kato_code` (механизм Story 6.3, не зависящий от `district_id`). `district_id` — подготовка для будущего прямого потребителя (карта, Story 3.4: цветовая маркировка по району и т.п.), уже читается обратно `store/geo.go`'s `ListGeoObjectsInBBox`. Когда 3.4 материализуется — свериться, действительно ли нужен именно `district_id`, а не повторный вывод из КАТО на лету.
- **`cmd/geocode`-интеграционные тесты в Makefile-группе `INTEG_CLEAN_PKGS`, а не `INTEG_SEED_PKGS`** — эмпирически проверено дважды (фейково-seeded и по-настоящему чистая БД): проходят корректно в ОБОИХ состояниях. Защитимая категоризация (тесты действительно НЕ требуют seed), но отличается от прецедента `districts_integration_test.go` (тоже self-contained, но живёт в SEED-группе). Не функциональный баг — организационная непоследовательность, стоящая внимания при следующей ревизии clean/seed-групп.

## Deferred from: code review of 3-2-полуручная-разметка-через-directus-fr-5 (2026-07-02)

- **Fail-fast секретов Directus в compose вне dev** — `deploy/docker-compose.cold.yml` подставляет тихие dev-дефолты (`SECRET: dev-only-secret-change-me`, `ADMIN_PASSWORD: change-me`) при незаполненном `.env`; синтаксис `${VAR:?}` не используется — прод-запуск профиля с забытым `.env` молча получает известные секреты (смягчено loopback-биндингом). Dev-дефолты санкционированы Task 4 истории 3.2 (паттерн POSTGRES_*), `.env.example` предупреждает. Ужесточение (fail-fast/отдельный prod-override) — ops-решение при подготовке пилота, бандл с role-DSN разводкой (deferred:269) и прод-экспозицией Directus (D4).
- **Аудит-след верификации гео для атрибуции SM-C2** — `MarkGeoObjectVerified`/`MarkGeoObjectWrongReported` меняют только `geocode_status` (ни кто, ни когда); `ClearGeoObjectToUnmatched` перезаписывает `geocoded_by` виновной точки (провенанс ошибочной геопривязки теряется после снятия). Доля SM-C2 вычислима, но восстановить «когда помечена/кем подтверждена» нельзя. Решить вместе с Epic-3-проводом error_reports→wrong_reported (deferred:69) — там же появится естественное место для журнала (error_reports уже несёт created_at) либо колонок verified_at/verified_by.
- **БД-гард переходов статусов на Directus-пути записи (решение владельца 2026-07-02: принята дисциплина чек-листа до живого пилота)** — Directus пишет `geo_objects` напрямую, мимо Go-гардов: dropdown позволяет любой переход (в т.ч. `manual`/`verified` → `auto`, после чего batch легально перезапишет кураторскую геометрию через гейт `IN ('auto','unmatched')`); правка geom у `verified` не сбрасывает статус; прямая правка geom сохраняет чужой Nominatim-`confidence`. Защита сейчас = чек-лист оператора (ops-док) + Go-методы (спека 3.2, D5). Hardening ДО живого пилота (бандл с role-DSN deferred:269 и прод-экспозицией D4): транзишн-триггер BEFORE UPDATE (белый список переходов D1 + batch-переходы + сброс confidence при ручной правке geom) ИЛИ Directus-permissions (роль куратора без права менять статус на auto / без правки verified). Найдено Blind+Edge Hunter (code-review 3.2).

## Deferred from: code review of 3-4-карта-с-маркерами-кластерами-и-линиями-fr-7 (2026-07-02)

- **`CountContractsWithoutPoint` — city-wide агрегат на каждый bbox-запрос без кэша/TTL** — `MapObjectsHandler.List` (`server/internal/httpapi/map_objects.go:90`) выполняет full-scan счётчик «без точки» (не зависящий от bbox) на каждый debounced-запрос панорамы (~300мс). На пилотной плотности (сотни–тысячи контрактов) несущественно (прецедент D1 compute-on-read 6.4); при реальных объёмах — кэш/TTL или отдельный редко-обновляемый эндпоинт. Найдено Blind Hunter (code-review 3.4).
- **Счётчик «вся Астана» без КАТО-фильтра** — `CountContractsWithoutPoint` (`server/internal/store/geo.go:131-136`) считает ВСЕ живые контракты без точки, без `kato_code LIKE '71%'`; семантика D4 «вся Астана» держится на пилотной посылке «в БД только Астана». Реализовано-как-специфицировано (буквальный SQL субтаска Task 1). Семейство отложенного решения «город=71» (медиана 6.4) — решить бандлом в Epic-3-go-live кластере. Найдено Acceptance Auditor (code-review 3.4).
- **A11y канонической карты: линии и фокус при рекластеризации → Story 3.8** — canvas-линии не имеют клавиатурной/скринридер-аффордансы (маркеры-точки — честные `<button>`, LINESTRING — только клик по canvas через queryRenderedFeatures); teardown DOM-маркеров при рекластеризации (moveend/refetch) сбрасывает клавиатурный фокус на body (`web/src/features/map/MapView.tsx`). Граница «полный a11y-проход = 3.8» зафиксирована спекой 3.4 (DOM-маркеры = прямой задел). Найдено Blind+Edge Hunter (code-review 3.4).
- **Смена темы не перекрашивает canvas-линии** — `mapToken(...)` читается однократно при `addLayer` (`web/src/features/map/MapView.tsx:214-236`); при переключении темы DOM-маркеры перекрашиваются CSS-переменными, линии остаются в цветах старой темы до пересоздания карты. Фронт-полировка единым проходом в конце проекта (решение владельца 2026-07-01). Найдено Edge Case Hunter (code-review 3.4).
- **Объекты с идентичными координатами не разводятся (spiderfy нет)** — несколько объектов на одном адресе: `getClusterExpansionZoom` за `CLUSTER_MAX_ZOOM` не разъединяет точки, маркеры перекрываются полностью — нижние невозможно выбрать тапом (`web/src/features/map/MapView.tsx:274`). Актуальность зависит от реальных данных (частота адресных дублей) — решить при живых geo_objects / в 3.8. Найдено Edge Case Hunter (code-review 3.4).
- **DESIGN.md-токены линий разошлись с реализацией** — dasharray `[2,1.5]` vs токен `stroke-dasharray: '5 6'` (DESIGN.md:245); `casing-selected: primary-strong` (DESIGN.md:247) не реализован — при выборе меняется только stroke на accent; кольцо выбранного маркера accent, тогда как DESIGN.md:237 задаёт primary-strong (EXPERIENCE.md:219 говорит accent — источники конфликтуют). Текст AC1 выполнен. Свести DESIGN↔EXPERIENCE↔код в фронт-полировку единым проходом. Найдено Acceptance Auditor (code-review 3.4).
- **Флаг-сигнал у LINESTRING не отображается (решение владельца 2026-07-02: отложить)** — линия дороги с `has_active_flag=true` визуально неотличима от обычной: canvas-paint кодирует только `selected` (accent/primary), глифы «!»/амбер-кольцо — только у DOM-точек и кластеров (`web/src/features/map/MapView.tsx:214-236`); линии не входят в supercluster-индекс. Спека 3.4 (D3/Task 4) для линий описывает лишь casing/пунктир/selected — трактовка флаг-линии не специфицирована. Латентно: на пилотном seed флаги только у DEMO-0001/0002 (без гео-строк), проявится на реальных данных Epic 3. Закрыть бандлом Epic-3-go-live/3.8: амбер stroke/casing через feature-state `hasFlag` + не-цветовой признак (WCAG 1.4.1 — напр. изменённый пунктир или DOM-глиф на линии). Найдено Blind Hunter (code-review 3.4).
