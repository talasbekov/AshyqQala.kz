# Ретроспектива — Epic 6: Поиск, фильтрация и страница района

**Дата:** 2026-06-30
**Фасилитатор:** Amelia (Developer) · **Project Lead:** Bratan
**Реализованные FR:** FR-15, FR-16, FR-17, FR-18
**Статус эпика:** завершён (6-1…6-4 = done)

---

## Сводка и метрики

| Story | FR | Итог | Ревью |
|---|---|---|---|
| 6.1 Фильтрация контрактов | FR-15 | done + коммит `fd2d01d` | code-review (patch-фиксы) |
| 6.2 Поиск БИН/наименование | FR-16 | done + коммит `1815f51` | code-review: D1 interleave, D2 нейтр.метка, 4 patch |
| 6.3 Агрегаты района | FR-17 | done (working tree `story/6-3`) | code-review (defer'ы в deferred-work) |
| 6.4 Медиана ₸/км | FR-18 | done (working tree) | code-review 2026-06-30: 3 patch + 1 defer, 9 dismiss |

- **Доставка:** 4/4 истории (100%). Блокеров уровня «стоп» — 0. Production-инцидентов — 0 (проект пред-продакшен).
- **Гардрейл-честность:** выдержал 3-слойную адверсариальную атаку (Blind / Edge / Acceptance) в каждой истории. Ни одной фабрикации не прошло.

---

## Что шло хорошо

1. **Дозрел паттерн «токен-независимый build».** 6.4 — самый чистый пример: полный движок сравнения `benchmark/comparison.go` (`Outcome`/`Evaluate`/`CompareGroups`) + честно-пустой адаптер `PricePerKMSamples→(nil,false)`; живые числа (`length_km`) отложены до Epic 3. Отработанный приём 2.0/4.1/5.1, без импровизации.
2. **Дисциплина «стражи обязаны краснеть» (lesson ретро Epic 4) закрепилась.** Property-тест AC3 с negative-control (n=4/n=5), `-count=1`, golden-литералы — применены в 6.2/6.3/6.4.
3. **Третья ось честности `container_state` (6.3)** закрыла дыру «пусто ≠ всё чисто» на уровне страницы района, не только полей (AR-17).
4. **Поиск 6.2 — корректный язык-агностик:** pg_trgm GIN + `simple` FTS (без `russian`-стеммера на KZ), БИН точный матч; UTF-8-коллация складывает казахские буквы (ICU не понадобилась).

## Что было трудно

1. **Entanglement 6.3/6.4.** Делят файлы `district/*` → ревью 6.4 в отрыве невозможно (упёрлись на code-review 6.4). Следствие того, что 6.3 не закоммичена отдельной серией.
2. **Пред-существующие CI-красные гейты всплыли и висят:** (a) web-e2e карточки контракта (`contract.spec.ts`/`flag.spec.ts`) красный с Epic 5 (RouteError вместо данных); (b) prettier-долг на ~18 закоммиченных файлах (5.1/5.2/6.1). Оба в `ci-web.yml` блокируют merge web-изменений.
3. **Commit-гигиена / дрейф ветки.** Гигантский незакоммиченный объём на `story/6-3` (6-3 + 6-4); имя ветки не отражает содержимое — операционный долг тянется не первую сессию.

---

## Преемственность с прошлыми ретро

- **Ретро Epic 4 → lesson «guards-must-prove-red»:** ✅ применён в 6.2/6.3/6.4 (property-тесты с negative-control, `-count=1`). Обещание выполнено.
- Ретро Epic 5 на диске нет (был optional, пропущен) — преемственность велась через `deferred-work.md` и memory.

---

## Подготовка к Epic 7 (Telegram-подписки, FR-24/25/26)

Порядок историй: 7.1 Dispatcher → 7.2 подписка+уведомления → 7.3 смена/отписка → 7.4 антифлуд/дайджест (7.1 до 7.2, нет forward-зависимостей).

- **7.1 (Dispatcher) — токен-НЕЗАВИСИМ, dev-able сейчас.** Опирается на `Dispatcher`-контракт outbox 2.7 (done) + lexicon/render Epic 4 (done). `TelegramDispatcher` = та же сигнатура (ноль правок 2.7); per-surface neutrality-тест на mock-транспорте. **Живая доставка** (polling, реальный чат, bot-bootstrap 3.7) = токен/Epic 3 — отложить (паттерн медианы 6.4).
- **7.2–7.4 — токен-блокированы** (bot-bootstrap 3.7 из Epic 3). Не запускать вслепую.
- **Хэндофф Epic 6 → 7:** district-поверхность (6.3/6.4) = цель deep-link'ов FR-24 уведомлений. Готова.
- **Значимых открытий, требующих переписать план Epic 7 — НЕТ.** Эпик идёт частично (7.1 сейчас, остальное ждёт токен) — известный паттерн.

---

## Action Items

### 🔴 Технический долг / БЛОКЕР
- **AI-1.** Очистить CI-красные гейты ДО merge web-изменений Epic 6: (a) `prettier --write .` отдельным chore-коммитом (~18 файлов 5.1/5.2/6.1); (b) диагностировать+починить RouteError карточки контракта (`contract.spec.ts`/`flag.spec.ts`). **Owner:** Bratan. **Success:** `ci-web.yml` (format:check + Playwright) зелёный.

### 🟡 Процесс
- **AI-2.** Закоммитить `story/6-3` + 6-4 серией по историям (+ отдельный chore prettier); имя ветки привести к содержимому. **Owner:** Bratan. **Success:** working tree чист, per-story коммиты.
- **AI-3.** *Team-agreement:* коммитить per-story сразу после code-review, не накапливать — чтобы следующее ревью было изолируемым (урок entanglement 6.3/6.4).

### 🔵 Планирование
- **AI-4.** Зафиксировать «**Epic-3-go-live кластер**»: при появлении `length_km` одновременно оживут флаг цена/км (4.3), медиана района (6.4) + отложенное решение семантики «город»-базы (superset / self-comparison `/districts/71` / не-Астанинский КАТО). Внести чек-лист активации в план Epic 3. **Owner:** Bratan (planning).

---

## Оценка готовности Epic 6

| Ось | Статус |
|---|---|
| Тесты/качество | Story-гейты зелёные (go test, vitest 123, playwright district 4/4); CI web глобально красный из-за пред-долга → AI-1 |
| Деплой | N/A (проект пред-продакшен) |
| Приёмка | Owner-приёмка через 3-слойные ревью — ✅ |
| Тех-здоровье | Стабильно; долги = entanglement + незакоммиченный объём |
| Блокеры | 2 критических до Epic 7 web-работы: очистить CI-гейты (AI-1) + закоммитить (AI-2) |

**Вывод:** Epic 6 завершён на уровне историй; перед web-работой Epic 7 — 2 критических пункта (AI-1, AI-2). 7.1 (dispatcher, backend) токен-независим и dev-able параллельно.

---

## Ключевые takeaways

1. Токен-независимый build + честно-пустой адаптер — теперь надёжный, повторяемый дефолт (подтверждён на 6.4 = медиана).
2. «Стражи обязаны краснеть» работает — переносим в Epic 7 (per-surface neutrality-тест на ФИНАЛЬНОЙ строке после Telegram-форматирования).
3. Не коммитить per-story сразу → следующее ревью становится неизолируемым (entanglement-налог). Чинить привычкой (AI-3).
4. Пред-существующий CI-долг — реальный merge-блокер; держать `ci-web` зелёным отдельным chore-коммитом до фич-merge.
