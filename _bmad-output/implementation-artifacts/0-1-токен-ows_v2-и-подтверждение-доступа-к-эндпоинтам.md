# Story 0.1: Токен ows_v2 и подтверждение доступа к эндпоинтам

Status: ready-for-dev

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a **команда платформы AshyqQala.kz**,
I want **получить `GOSZAKUP_TOKEN` и подтвердить доступ к нужным эндпоинтам ows_v2**,
so that **гейт №0 и будущий импорт работают на официальном канале (ows_v2 по токену), а не на парсинге портала** (несущий принцип проекта).

> **Тип истории:** enabler-story Эпика 0 (без FR). Снимает **Блокер №0** (наличие токена) и
> является воротами для Epic 2 (импорт) и Epic 3 (геопривязка). Несёт AR-2.
> **Важно:** часть AC — это человеко-операционные действия (получение токена через
> профиль участника goszakup.gov.kz, юр-изоляция доступа), которые dev-агент НЕ может
> выполнить за человека. Dev-агент отвечает за: проверяемую часть (200 OK по эндпоинтам
> через инструмент), фиксацию КАТО-карты районов, определение фильтра направлений на
> выборке и документирование результатов. Действия, требующие человека, помечены 👤.

## Acceptance Criteria

**AC1 — Токен получен и изолирован** 👤
**Given** профиль участника на goszakup.gov.kz
**When** запрошен токен ows_v2
**Then** токен получен и хранится в секрет-сторе VPS (вне репозитория), доступ изолирован.

**AC2 — Эндпоинты отвечают 200 OK**
**Given** валидный токен
**When** отправлен тестовый запрос к `/v2/journal`, договорам (`/contract`), лотам (`/lots`),
`/v2/trd-buy`, участникам, `/v2/rnu`, `/v2/acts`
**Then** каждый эндпоинт отвечает `200 OK`.

**AC3 — Зафиксирована КАТО-карта районов Астаны**
**And** список КАТО районов Астаны (Алматы, Сарыарка, Есиль, Байконыр, Нура) зафиксирован
(конкретные коды районов, не только префикс «71»).

**AC4 — Определён фильтр направлений на выборке**
**And** фильтр «дороги»/«водоснабжение» по предмету/коду закупки определён на выборке
(подтверждена применимость существующих ключевых слов и/или кодов закупки).

## Tasks / Subtasks

- [ ] **Task 1 — Получить и изолировать токен (AC1)** 👤 *(человеко-операционная часть; dev-агент только документирует факт и схему хранения)*
  - [ ] Назначить владельца, получить `GOSZAKUP_TOKEN` через профиль участника goszakup.gov.kz
  - [ ] Положить токен в секрет-стор VPS / `.env` вне репозитория; убедиться, что он **не** попадает в git (проверить `.gitignore`)
  - [ ] Зафиксировать в `docs/ops/stage0-access.md` способ хранения и ротации (БЕЗ самого значения токена), сослаться на принцип изоляции: токен видит только будущий `cmd/importer`, не `cmd/api`
- [ ] **Task 2 — Подтвердить 200 OK по всем эндпоинтам (AC2)**
  - [ ] Запустить `cd stage0-audit && go build ./... && go run . -probe` с экспортированным `GOSZAKUP_TOKEN` — это печатает реальные имена полей первого объекта каждого эндпоинта и подтверждает 200
  - [x] ✅ **РЕШЕНО (правка инструмента уже внесена):** `journal` добавлен в probe-цикл `stage0-audit/main.go` и в `source.go` (`case "journal"` → `/journal`). Дев просто запускает `-probe` и фиксирует фактический статус journal на живом токене. Точный контракт курсора/ретеншна — задача B-2 (Story 0.4), не этой истории
  - [x] ✅ **РЕШЕНО — «участники» это НЕ эндпоинт, а ПОЛЕ:** число участников для флага FR-19 читается полем из `trd-buy` (`main.go:347-366`, `getField(rec,"participants")`), а `/subject` — это реестр юрлиц. Probe теперь печатает блок «Резолв поля число участников» и называет сматченного кандидата (на фикстурах — `count` в `trd-buy`). Дев фиксирует РЕАЛЬНОЕ имя поля с живого токена; если ни один кандидат не сматчился — добавляет имя в `fieldCandidates["participants"]` (`models.go`), не меняя логику
  - [ ] Записать таблицу «эндпоинт → статус» (включая journal и поле числа участников из trd-buy) в `docs/ops/stage0-access.md`
- [ ] **Task 3 — Зафиксировать КАТО-коды районов Астаны (AC3)**
  - [ ] Подтвердить префикс города «71» (`stage0-audit/config.go:33`, `AstanaKATOPrefixes`) против реальных данных через `-probe`
  - [ ] Получить/подтвердить конкретные КАТО-коды пяти районов (Алматы, Сарыарка, Есиль, Байконыр, Нура) из живого API и/или официального классификатора КАТО; **не выдумывать коды** — если код не подтверждён, пометить «нет данных» и поставить вопрос владельцу
  - [ ] Записать карту «район → КАТО» в `docs/ops/stage0-access.md`
- [ ] **Task 4 — Определить фильтр направлений на выборке (AC4)**
  - [ ] На выборке лотов/договоров проверить применимость существующих списков `RoadKeywords` / `WaterKeywords` (`stage0-audit/config.go`) к полю предмета закупки
  - [ ] Оценить, нужен ли дополнительно фильтр по **коду закупки** (текущая классификация — только по тексту); зафиксировать решение и долю покрытия на выборке
  - [ ] Записать определение фильтра (поля, ключевые слова/коды, доля совпадений на выборке) в `docs/ops/stage0-access.md`
- [ ] **Task 5 — Финализация**
  - [ ] Убедиться, что `docs/ops/stage0-access.md` содержит: схему хранения токена, таблицу 200-OK, КАТО-карту районов, определение фильтра направлений
  - [ ] Открытые/неподтверждённые пункты явно помечены «нет данных» / поставлены как вопросы (честность важнее догадок)

## Dev Notes

### Контекст истории (зачем это первым)

Эпик 0 снимает ведущий риск проекта (по Минто): **данных по Астане (дороги+вода) физически
достаточно и они геокодируемы** (OQ-1 × OQ-4 × OQ-6 — один риск в трёх проекциях). Детектор
уже написан — это инструмент `stage0-audit/`. **Блокер №0 — наличие `GOSZAKUP_TOKEN`.** Эта
история открывает официальный канал доступа; всё остальное в Эпике 0 (0.2 машиночитаемый
вердикт, 0.3 прогон на живых данных, 0.4 предполётные решения) зависит от неё.
[Source: _bmad-output/planning-artifacts/architecture.md#Гейт-№0 (строки 162–191)]
[Source: _bmad-output/planning-artifacts/epics.md#Epic-0]

### Что переиспользовать (НЕ изобретать заново)

**Инструмент уже существует — `stage0-audit/` (Go, только stdlib, модуль `ashyqqala/stage0-audit`, Go 1.25).**
Эта история НЕ создаёт новый код доступа к API — она использует существующий инструмент и,
при необходимости, делает в нём минимальную правку (добавить `journal` в probe).

- **Запуск (всегда `-probe` первым):**
  ```bash
  cd stage0-audit
  go build ./...
  export GOSZAKUP_TOKEN="<токен ows_v2 Bearer>"
  go run . -probe        # печатает реальные имена полей каждого эндпоинта + подтверждает 200
  ```
- **Базовый URL:** `https://ows.goszakup.gov.kz/v2` — дефолт флага `-base`
  [Source: stage0-audit/main.go:14]
- **Реальные пути ресурсов** (то, что инструмент уже умеет дёргать):
  `/contract`, `/lots`, `/trd-buy`, `/rnu`, `/subject`, `/acts`; по БИН:
  `/contract/customer/{bin}`, `/lots/bin/{bin}`
  [Source: stage0-audit/source.go:37–78]
- **Probe-цикл ресурсов:** `["journal","contract","lots","trd-buy","subject","rnu","acts"]`
  [Source: stage0-audit/main.go] — `journal` добавлен; после дампа печатается резолв поля
  числа участников (`resolveParticipantField`)
- **КАТО-префикс Астаны:** `AstanaKATOPrefixes = []string{"71"}` — помечен VERIFY
  [Source: stage0-audit/config.go:33]
- **Ключевые слова направлений:** `RoadKeywords`, `WaterKeywords` (рус+каз) уже определены —
  переиспользовать и валидировать на выборке, а не писать заново
  [Source: stage0-audit/config.go]
- **Astana viewbox** для геокодера: `71.20,51.30,71.78,51.00` [Source: stage0-audit/config.go:30]

### Соблюдение архитектуры (guardrails)

- **Официальный канал, не парсинг.** Доступ — только `ows_v2` по токену. `-source scrape`
  существует, но **юридически ограничен** (нарушает несущий принцип §6.1/§6.4) — НЕ использовать
  для этой истории. [Source: _bmad-output/planning-artifacts/architecture.md строки 94, 100]
- **Изоляция токена (несущий инвариант на будущее).** В целевой архитектуре `cmd/importer`
  держит `GOSZAKUP_TOKEN` и пишет; `cmd/api` токена **не видит**. Секреты — env/секрет-стор
  VPS, вне репо, с ротацией. Документировать AC1 в этих терминах.
  [Source: _bmad-output/planning-artifacts/architecture.md строки 100, 390–391]
- **Схемная устойчивость (намеренная).** Парсер `stage0-audit` никогда не предполагает точные
  имена полей API: `models.go` маппит логическое поле на список кандидатов (`fieldCandidates`),
  первый непустой выигрывает; неизвестная схема даёт `""`/`0` и честно показывается в метриках
  полноты, без падения. Если `-probe` показал ДРУГОЕ имя поля — **добавить кандидата** в список,
  а не менять логику. [Source: CLAUDE.md «Key conventions», stage0-audit/models.go]
- **VERIFY-маркеры** в `config.go`/`models.go` — это допущения, которые именно сейчас (на живом
  токене) надо подтвердить: префикс КАТО «71», имена полей суммы/БИН/дат/КАТО/anno, и **источник
  числа участников** для флага «единственный участник». [Source: CLAUDE.md, stage0-audit/models.go]
- **Честность над домыслом.** Неподтверждённое (например, точный КАТО-код района) фиксируется
  как «нет данных» / вопрос владельцу. Не интерполировать и не выдумывать.

### Файлы и куда писать результат

- **Целевой артефакт документации:** `docs/ops/stage0-access.md` (каталог `docs/ops/` пока **не
  существует** — создать). Соседняя история 0.3 пишет в `docs/ops/stage0-gate.md`, так что
  каталог `docs/ops/` — правильное место. Снапшот контракта схемы (`docs/api-contracts/ows_v2-*.json`)
  — это область истории **0.4 (B-1)**, НЕ 0.1; здесь не дублировать.
- **Правка инструмента уже внесена** (в рамках разрешения пробелов AC2): `stage0-audit/main.go`
  (`journal` в probe-цикле + функция `resolveParticipantField`), `stage0-audit/source.go`
  (`case "journal"` → `/journal`; уточнён комментарий `/subject`), `stage0-audit/models.go`
  (комментарий к `fieldCandidates["participants"]`). Проверено: `go build ./...` + `go vet ./...`
  зелёные; `-probe` на фикстурах подтверждает резолв `trd-buy → "count"`. Тестов и Makefile в
  `stage0-audit/` нет — `go build ./...` единственная проверка сборки.
- **Не трогать** прикладной MVP-код (`server/`, `web/` и т.п.) — его ещё не существует; целевой
  стек (Go 1.25 + chi, pgx+sqlc, Postgres 16+PostGIS, React+Vite+MapLibre) появится в Epic 1+.

### Тестирование / проверка готовности

- **Нет юнит-тестов** в `stage0-audit` — критерий приёмки здесь операционно-документальный, не
  через тест-сьют. Проверка = воспроизводимый прогон `go run . -probe` с фактическими статусами
  200 и заполненный `docs/ops/stage0-access.md`.
- DoD истории: AC1–AC4 закрыты; каждый эндпоинт из AC2 имеет зафиксированный статус (включая
  `journal` и подтверждённый эндпоинт участников); КАТО-карта 5 районов и определение фильтра
  записаны; неподтверждённое явно помечено.

### Внешние знания / web-research (осознанно ограничено)

История не вводит новых библиотек (используется существующий stdlib-инструмент), поэтому
research версий не требуется. Специфику живого API (формат Bearer-заголовка, курсор/ретеншн
`/v2/journal`, точные КАТО-коды) **намеренно не берём из памяти модели** — она подтверждается
против реального токена через `-probe`/`curl` и официальную документацию goszakup, согласно
принципу проекта «подтверждай, не предполагай». Детальное описание `/v2/journal` (курсор,
гранулярность, ретеншн — B-2) — задача истории 0.4; здесь достаточно подтвердить 200 OK.

### Git-контекст

В репозитории сейчас только скаффолд-коммиты (`Initial commit`, `Claude /init`); прикладного кода,
наследуемых паттернов или предыдущей истории нет — это первая история первого эпика. Опираться
не на git-историю, а на `stage0-audit/` и планировочные артефакты.

### Project Structure Notes

- Артефакт истории кладётся в `docs/ops/` (создаётся), согласуясь с `docs/ops/stage0-gate.md` из 0.3.
- Любая правка кода ограничена существующим модулем `stage0-audit/`; целевое монорепо (`server/`,
  `web/`, `deploy/`, `migrations/`, `registry/`, `fixtures/`) — предмет Story 1.1, здесь не создаётся.
- Конфликтов с целевой структурой нет: enabler-история работает поверх существующего инструмента.

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Epic-0 → Story-0.1 (строки 636–657)]
- [Source: _bmad-output/planning-artifacts/architecture.md#Гейт-№0 (строки 162–191)]
- [Source: _bmad-output/planning-artifacts/architecture.md — Изоляция токена / источник данных (строки 94, 100, 390–391)]
- [Source: stage0-audit/main.go (base URL :14; probe-цикл :123)]
- [Source: stage0-audit/source.go (пути ресурсов :37–78)]
- [Source: stage0-audit/config.go (КАТО-префикс :33; viewbox :30; RoadKeywords/WaterKeywords)]
- [Source: stage0-audit/models.go (fieldCandidates; VERIFY — источник числа участников)]
- [Source: CLAUDE.md — «-probe first», VERIFY-маркеры, схемная устойчивость, гардрейл честности]
- [Source: docs/AshyqQala_stage0_data_audit_runbook_v1.md — runbook этапа 0]

## Dev Agent Record

### Agent Model Used

_(заполняется dev-агентом)_

### Debug Log References

### Completion Notes List

- Создано context engine: comprehensive developer guide — анализ epics/architecture/кода stage0-audit завершён.

### File List
