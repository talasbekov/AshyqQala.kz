-- +goose Up
-- Story 3.1 Task 2: уникальность geo_objects.contract_id (частичная, NULL допустим повторяться) — нужна
-- для идемпотентного batch-геокодинга (UPSERT ... ON CONFLICT (contract_id)). Один контракт = один
-- канонический geo_object; повторный геокод ОБНОВЛЯЕТ существующую строку, не плодит дубли. NULL остаётся
-- неуникальным (прецедент 0020 partial-unique districts_kato_uniq: contract_id nullable для будущих
-- lot-keyed/безконтрактных источников — здесь их нет, но колонка уже nullable в 0020, индекс это уважает).
--
-- ⚠️ Код-ревью: БЕЗ CONCURRENTLY → ACCESS EXCLUSIVE лок на geo_objects на время построения индекса.
-- Приемлемо здесь: таблица новая (0020, эта же история), пуста/только-seed, продакшн-трафика ещё нет
-- (пре-запуск, токен не получен). Если эта миграция когда-либо переигрывается на таблице с живым batch-
-- трафиком — построение индекса CONCURRENTLY вне транзакции (отдельная goose-аннотация для no-transaction
-- режима) станет обязательным; сейчас это НЕ сделано умышленно (не усложнять миграцию ради риска, которого
-- пока нет). ПРИМЕЧАНИЕ: не писать в этом файле буквальный текст "плюс-goose NO TRANSACTION" — goose
-- парсит ЛЮБУЮ строку с этим префиксом как директиву, даже внутри обычного prose-комментария (эта
-- миграция один раз уже ловила разбор-ошибку ровно на этом).
CREATE UNIQUE INDEX geo_objects_contract_uniq ON geo_objects (contract_id) WHERE contract_id IS NOT NULL;

-- +goose Down
DROP INDEX IF EXISTS geo_objects_contract_uniq;
