# Caddy reverse-proxy — каркас S-0. Полная конфигурация (SPA + /api → cmd/api + /tiles pmtiles)
# наполняется в Story 1.7 (DoD walking skeleton: байт Postgres→sqlc→chi→Caddy→React).
:80 {
	# /api/* → api:8080 (Story 1.3); статика web/dist + /tiles/*.pmtiles (Story 1.7/1.8)
	respond "AshyqQala.kz — caddy skeleton (S-0). API/SPA wiring: Story 1.7." 200
}
